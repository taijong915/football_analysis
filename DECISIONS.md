# DECISIONS.md

이 파일은 이 프로젝트에서 내린 주요 의사결정을 시간순으로 기록합니다. 새로운 결정을 내리면 아래 형식으로 항목을 추가하세요.

```
## YYYY-MM-DD 결정 제목

**결정**: 무엇을 하기로 했는지
**이유**: 왜 그렇게 결정했는지 (대안이 있었다면 함께 기록)
```

---

## 2026-08-21 CLAUDE.md 최초 작성

**결정**: `CLAUDE.md`를 신규 작성하여 빌드/실행 명령어와 아키텍처 개요(`data_loader.py` → `visualizer.py` → 스크립트/노트북 파이프라인)를 문서화했다.
**이유**: 기존에 Claude Code용 가이드 문서, Cursor/Copilot 규칙, Codex/Gemini 설정이 전혀 없어 매번 코드베이스를 처음부터 파악해야 했다.

## 2026-08-21 CLAUDE.md를 한글로 작성

**결정**: `CLAUDE.md`의 설명 문구를 전부 한글로 번역했다 (명령어, 경로, 함수명 등 코드 관련 요소는 원문 유지).
**이유**: 소스 코드 주석, 독스트링, README가 모두 한글로 작성되어 있어 문서 스타일을 프로젝트 전체와 일관되게 맞추기 위함.

## 2026-08-21 기존 GitHub 저장소(taijong915/football_analysis)에 연결

**결정**: 로컬 프로젝트를 `git init`으로 초기화한 뒤, 이미 존재하던 원격 저장소 `https://github.com/taijong915/football_analysis`를 `origin`으로 추가하고 연결했다. 새 저장소를 만들지 않았다.
**이유**: 사용자가 해당 URL의 기존 저장소를 지정했다. 기존 저장소에는 placeholder README 한 줄(`# football_analysis`)만 있었고 실질적인 코드가 없어 충돌 없이 로컬 내용을 이식할 수 있었다.

## 2026-08-21 원격/로컬 히스토리 병합 시 로컬 README를 우선

**결정**: `git merge origin/main --allow-unrelated-histories -X ours`로 병합하여, 원격의 placeholder README 대신 로컬의 상세 README(`README.md`)를 유지했다.
**이유**: 두 히스토리가 공통 조상이 없는 별개 저장소였고(unrelated histories), 유일한 충돌 파일인 README.md는 로컬 버전이 압도적으로 더 상세했다. 강제 push(`--force`) 대신 정상적인 병합 커밋으로 이력을 보존하는 방식을 택했다.

## 2026-08-21 분석 주제 아이디어를 위한 `ideas/` 폴더 신설

**결정**: 분석 주제를 브레인스토밍하고 백로그로 관리하는 `ideas/` 폴더를 만들었다. 상태(대기/구체화/진행중/완료/보류)를 가진 단일 백로그 파일(`ideas/backlog.md`)과 사용법을 설명하는 `ideas/README.md`로 구성했다. 주제별로 파일을 쪼개지 않았다.
**이유**: 아직 아이디어가 소수일 것으로 예상되어 파일을 여러 개로 나누는 것은 과도한 구조화였다. 노트북/스크립트로 옮겨가기 전 단계의 논의를 코드/문서와 분리해 `notebooks/`, `scripts/`가 실제 완료된 분석으로만 채워지도록 유지하려 했다.

## 2026-08-21 아이디어 도출 시 질문증강 방식으로 소통하도록 명시

**결정**: `CLAUDE.md`에 "아이디어 도출 시 역할" 섹션을 추가하여, `ideas/` 관련 대화에서는 아이디어를 바로 나열하지 않고 질문증강(question augmentation) 방식 - 되묻는 질문으로 사용자의 생각을 구체화시키는 방식 - 으로 소통하도록 명시했다. 단, 사용자가 명시적으로 브레인스토밍 목록을 요청하면 예외로 뒀다.
**이유**: 사용자가 아이디어 도출 과정 자체를 대화형으로 진행하길 원했다. Claude가 일방적으로 아이디어를 나열하는 대신, 질문을 통해 사용자가 스스로 관심사와 제약을 명확히 하도록 돕는 것이 목표다.

## 2026-08-21 .gitignore는 기존 파일을 그대로 사용

**결정**: 새로 작성하지 않고 프로젝트에 이미 있던 `.gitignore`(`.venv/`, `__pycache__/`, `.ipynb_checkpoints` 등 제외 규칙 포함)를 그대로 사용해 첫 커밋을 진행했다.
**이유**: 기존 `.gitignore`가 이미 이 프로젝트에 필요한 항목(가상환경, 파이썬 캐시, 노트북 체크포인트, IDE 설정)을 적절히 커버하고 있어 중복 작업이 불필요했다. `data/raw|processed`는 주석 처리된 상태로 두어 현재는 결과 이미지가 저장소에 커밋되도록 유지했다.

## 2026-08-22 아이디어 도출: 2024 유로 스페인 빌드업 패턴 주제로 착수

**결정**: 질문증강 방식으로 논의한 끝에 "2024 유로 스페인 빌드업 패턴"(핵심 선수 패스 네트워크 + 구역 기반 전진 경로)을 `ideas/backlog.md`에 구체화 → 진행중으로 등록하고, 우선 패스 네트워크 부분부터 착수했다. 구역 기반 전진 경로 분석과 7경기 전체 실행(`notebooks/04_spain_euro2024_buildup.ipynb`)은 다음으로 미뤘다.
**이유**: 사용자가 전술적 흐름 중에서도 "한 팀이 대회 전체에 걸쳐 보이는 경향"과 "빌드업 패턴"을 선택했고, 샘플로 방법론부터 검증하고 싶어했다. StatsBomb 오픈 데이터(현재 저장소명 `hudl/open-data`)에 2026 북중미 월드컵은 아직 없고(마지막 커밋이 2026-05-26으로 대회 시작 전 시점에 멈춰 있음), 최신 클럽 시즌(FBref 등)은 좌표 기반 이벤트 데이터가 없어 2024 유로가 최신성과 이벤트 데이터 요건을 동시에 만족하는 선택지였다.

## 2026-08-22 패스 네트워크: 첫 교체 이전 구간 + player_nickname 사용

**결정**: `src/visualizer.py`의 `plot_pass_network()`는 기본적으로 해당 팀의 첫 교체 시각 이전 데이터만 사용하고, 선수 라벨은 `player` 필드를 잘라 쓰는 대신 `lineups`의 `player_nickname`을 사용한다.
**이유**: 교체 이후에는 선발 11명 조합이 섞여 네트워크가 왜곡된다. 또한 스페인처럼 성이 두 개인 선수는 `player` 필드 마지막 단어만 잘라 쓰면 실제 통용되는 이름과 어긋난다 - 샘플 작업 중 실제로 `Daniel Carvajal Ramos`를 자르면 "Ramos"가 되어 `Daniel Olmo Carvajal`("Carvajal")과 표기가 서로 겹쳐 혼동되는 버그가 발견됐다.

## 2026-08-22 샘플 분석 결과는 노트북과 별도로 마크다운 문서화

**결정**: 샘플 패스 네트워크 분석 결과(가장 영향력 있는 선수, 대형 특징 등)를 `data/processed/spain_pass_network_euro2024_final_sample.md`에 정리하고 같은 이름의 PNG를 상대 경로로 임베드했다. 노트북의 "관찰 기록" 셀에 적지 않고 별도 파일로 분리했다.
**이유**: `notebooks/04_spain_euro2024_buildup.ipynb`는 아직 7경기 전체를 도는 스캐폴딩 단계이고, 이번 결과는 방법론 검증을 위한 1경기(결승전 전반) 샘플이라 즉시 참조 가능한 독립 문서로 남기는 편이 나았다.

## 2026-08-22 스페인 분석을 위한 전용 폴더 `spain_euro2024/` 신설

**결정**: `notebooks/04_spain_euro2024_buildup.ipynb`와 `data/processed/spain_pass_network_euro2024_final_sample.*`를 새로 만든 `spain_euro2024/` 폴더(노트북 최상위 + `processed/` 하위 산출물)로 이동하고, 분석 기획을 정리한 `spain_euro2024/PLAN.md`를 추가했다. `ideas/backlog.md`와 `CLAUDE.md` 아키텍처 섹션에서 이 폴더를 참조하도록 갱신했다.
**이유**: 이 분석은 7경기에 걸쳐 노트북 1개 + 산출물 다수(경기당 패스 네트워크 PNG, 추후 구역 기반 전진 경로 시각화 등)를 생성하는 다회차 작업이라, `notebooks/`·`data/processed/`에 흩어 두면 다른 예제/탐색 노트북과 섞여 추적하기 어려워진다. 팀/주제별 전용 폴더로 묶어 기획 문서·노트북·산출물을 한곳에서 관리하기로 했다.

## 2026-08-22 주제별 분석 표준 워크플로우를 CLAUDE.md에 명시

**결정**: `spain_euro2024/` 폴더 구성 방식을 일회성 예외가 아니라, 이 프로젝트에서 여러 주제를 분석하기 위한 표준 워크플로우로 승격했다. `CLAUDE.md`에 "분석 워크플로우" 섹션을 새로 만들어 3단계(① `ideas/backlog.md`에서 질문증강 방식으로 아이디어 논의 → ② 착수 시 주제별 전용 폴더 생성 + `PLAN.md` 작성 → ③ 해당 폴더의 `processed/` 안에서 산출물 관리)를 명시했다. 기존 스타터 자료(`notebooks/01~03`, `scripts/example_analysis.py`, `data/processed/`)는 이 워크플로우와 무관한 최초 셋업용 예제로 남겨두고 그대로 뒀다.
**이유**: 사용자가 이 프로젝트를 여러 분석 주제를 계속 이어가는 용도로 쓰겠다고 밝혔다. `spain_euro2024/`에서 검증한 폴더 구조(기획 문서 + 노트북 + 전용 산출물 폴더)를 매번 새로 논의하지 않고 반복 가능한 규칙으로 문서화해, 새 주제가 생길 때마다 일관된 구조로 착수하도록 했다.

## 2026-08-22 `notebooks/`·`scripts/`·`data/`를 함수 개발·테스트 샌드박스로 재정의

**결정**: 주제별 분석 산출물이 각자의 전용 폴더(`spain_euro2024/` 등)로 옮겨가면서, 프로젝트 공용 폴더로 남은 `notebooks/`, `scripts/`, `data/`의 역할을 "재사용 가능한 함수를 프로토타입·테스트하고, 검증되면 `src/`로 승격해 버전 관리하는 샌드박스"로 명시했다. `CLAUDE.md`에 "기능 개발/테스트 샌드박스" 절을 추가하고, 아키텍처 섹션의 `notebooks/`·`scripts/`·`data/processed/` 설명을 이에 맞춰 갱신했다.
**이유**: 주제 폴더 워크플로우가 자리 잡으면서 이 세 폴더가 "무엇을 위한 공간인지" 애매해졌다. 사용자가 이 폴더들을 (1) 분석에 쓰는 함수를 저장·버전관리하고 (2) 분석 중 새 기능을 구현할 때 테스트하는 공간으로 쓰고 싶다고 명시적으로 요청했다. `src/`가 이미 정식 함수 저장소 역할을 하므로, `notebooks/`·`scripts/`는 그 앞단(프로토타입 → 검증 → `src/` 승격) 역할로, `data/`는 테스트용 중간 산출물 저장소로 자리매김했다. 기존 스타터 노트북(`01`~`03`)·`example_analysis.py`는 최초 예제 자료로 그대로 유지했다.

## 2026-08-22 상세 가이드를 `.claude/rules/`로 분리해 `CLAUDE.md` 슬림화

**결정**: `CLAUDE.md`(당시 97줄)에서 자주 쓰이지 않는 상세 절차와 도메인 지식을 `.claude/rules/`로 옮겼다 - 분석 워크플로우 3단계 + 샌드박스 규칙의 세부 내용은 `.claude/rules/analysis-workflow.md`로, StatsBomb 이벤트 데이터 컬럼 규칙(좌표 언패킹, outcome 의미, 선수 이름/교체 처리)은 `.claude/rules/statsbomb-data-notes.md`로 이동했다. `CLAUDE.md`에는 각 절의 1~2문장 요약과 필요 시점(주제 착수 시 / `data_loader`·`visualizer` 수정 시)에 읽을 링크만 남겨 63줄로 줄였다.
**이유**: `CLAUDE.md`는 매 세션 항상 로드되는 반면, 워크플로우 세부 규칙이나 StatsBomb 컬럼 규칙은 실제로 해당 작업을 할 때만 필요하다. 사용자가 이 비대칭을 지적하며 상세 내용을 분리해 필요할 때만 읽도록 요청했다. `.claude/rules/*.md`는 자동 로드되지 않으므로, `CLAUDE.md`가 적절한 시점에 명시적으로 링크해 Claude가 그때 가서 읽도록 구성했다.

## 2026-08-22 CLAUDE.md 검토

**결정**: `CLAUDE.md`에 "CLAUDE.md 정기 검토" 절을 추가했다 - `DECISIONS.md`를 읽을 때마다 가장 최근 "CLAUDE.md 검토" 항목의 날짜를 확인해, 약 1개월 이상 지났거나 주제 폴더가 3개 이상 새로 누적됐거나 사용자가 명시적으로 요청하면 `CLAUDE.md`와 `.claude/rules/*.md`를 실제 프로젝트 구조와 대조해 검토하도록 했다. 이번 항목 자체가 첫 검토 기준점이다(이번엔 절 추가가 곧 검토 결과이므로 바뀐 내용 없음).
**이유**: 문서가 실제 구조와 조용히 어긋나는 것을 막으려면 "언제 다시 봐야 하는지"를 문서 자체에 명시해야 한다. `DECISIONS.md`를 매 세션 필독하는 기존 습관에 편승시켜, 별도 리마인더 없이도 검토 시점을 스스로 판단할 수 있게 했다.

## 2026-08-22 분석 워크플로우에 "데이터 검토" 단계 추가

**결정**: `.claude/rules/analysis-workflow.md`의 워크플로우를 3단계에서 4단계로 확장해, "아이디어 논의" 직후·"주제 폴더 구성" 직전에 "데이터 검토" 단계를 넣었다 - 방법론이 의존하는 데이터 필드(결측률, 값 분포, 관련 이벤트 발생 빈도 등)를 대상 경기/대회로 직접 확인하고, 결과를 주제 한정이면 `PLAN.md`에, 일반화 가능하면 `.claude/rules/statsbomb-data-notes.md`에 기록하도록 명시했다. `CLAUDE.md`의 요약 문장도 4단계로 갱신했다.
**이유**: 스페인 결승전 패스 네트워크를 교체 시각 기준으로 잘게 쪼개보니 짧은 구간(6분)에서는 표본 부족으로 품질이 떨어지는 문제가 나왔고, 대안으로 `position` 컬럼 기반 포지션 슬롯 집계를 검토했다. 이 컬럼이 실제로 쓸 만한지(결측률, 교체 시 라벨 유지 여부, `Tactical Shift`로 인한 라벨 변경 빈도)는 "감"이 아니라 대상 데이터로 직접 확인해야 하는 문제였다. 사용자가 이런 검증을 매번 애드혹하게 하지 말고 워크플로우의 정식 단계로 명시해두자고 요청했다 - 방법론을 코드로 짜기 전에 데이터를 먼저 검증하는 습관을 프로젝트 규칙으로 고정했다.

## 2026-08-22 포지션 기반 통합 패스 네트워크를 주 방법론으로 채택

**결정**: `src/visualizer.py`에 `plot_pass_network_by_position()`을 추가하고, 스페인 분석의 "경기 전체 양상을 보여주는" 주 방법론으로 채택했다. 기존 `plot_pass_network()`(선발 라인업/특정 구간 스냅샷)는 보조 방법론으로 남겼다. 새 함수는 노드를 선수 이름이 아니라 `position` 컬럼 기반 포지션 슬롯으로 잡아 교체와 무관하게 90분 전체 패스를 합산하고, 오른쪽 패널에 포지션별 선수 로스터 + 교체 시각(첫 등장 시각의 근사치)을 함께 표시해 "어떤 선수가 언제 그 슬롯을 맡았는지"를 보완한다. 워크플로우 규칙대로 `scripts/test_position_pass_network.py`에서 먼저 프로토타입·검증한 뒤 `src/`로 승격했고, 그 스크립트는 승격된 함수를 가져다 쓰는 형태로 남겨뒀다. `spain_euro2024/PLAN.md`의 방법론 요약·예상 산출물·진행 상황·한계 절도 함께 갱신했다.
**이유**: 사용자가 "position 통합 네트워크가 나아보인다"며 채택 의사를 밝혔고, 결과 이미지 옆에 선수 교체가 어떻게 이루어졌는지도 함께 보여달라고 요청했다. 처음 프로토타입(선수 이름을 그대로 노드 라벨로 사용)은 중앙 포지션들의 평균 위치가 가까워 라벨이 심하게 겹쳤고, 오른쪽 패널 제목에 한글을 쓰자 기본 matplotlib 폰트가 한글 글리프를 지원하지 못해 빈 사각형으로 깨졌다. 두 문제를 고쳐 노드는 포지션 약어만 표시하고 전체 로스터/교체 이력은 오른쪽 패널로 분리했으며, 플롯에 출력되는 텍스트는 기존 함수들과 동일하게 영어로 통일했다.

## 2026-08-22 7경기 전체 실행 후 발견한 두 가지 문제를 수정

**결정**: `plot_pass_network_by_position()`을 7경기 전체에 돌려보고 발견한 두 문제를 고쳤다. ① `min_node_pass_count`(기본값 3) 파라미터를 추가해, 후반 막판 몇 분만 뛰고 패스를 거의 못 만진 조커 교체 선수가 표본 부족으로 다른 노드와 겹치는 "가짜" 노드를 만드는 걸 막았다(프랑스전 93분 투입 수비멘디의 스퓨리어스 RCM 노드로 확인). ② 로스터 패널에서 "실제 `Substitution`으로 새로 들어온 선수"와 "`Tactical Shift`로 라벨만 바뀐, 이미 뛰고 있던 선수"를 구분하도록 고쳤다 - 각 선수의 "경기 전체 첫 이벤트 시각"과 "그 슬롯에서의 첫 등장 시각"을 비교해, 슬롯 등장이 더 늦으면 `*`를 붙이고 패널 하단에 범례를 추가했다. 두 수정 모두 7경기 전체를 재생성해 검증했고, `spain_euro2024/PLAN.md`의 방법론·진행 상황·한계 절을 갱신했다.
**이유**: 결승전 1경기만으로는 안 드러나던 문제들이 7경기 전체를 돌리자 나타났다 - 데이터 검토를 대상 경기 전체로 해야 하는 이유를 다시 확인시켜준 사례다. 특히 ②는 사용자가 프랑스전 결과를 직접 보고 "메리노가 76분에 올모와 교체되고 93분에 포지션이 바뀐 거냐"고 질문하면서 드러났다 - 로스터 패널의 "→" 표기가 실제 교체와 단순 재태깅을 구분 없이 보여줘서 오해를 살 수 있다는 게 확인됐고, 고친 뒤에는 오히려 나바스 교체 이후 나초가 RCB→RB로 재배치된 것 같은 실제 전술 변화까지 로스터 패널에서 읽어낼 수 있게 됐다.

## 2026-08-22 패스 네트워크 산출물을 `spain_euro2024/pass_network/` 하위 폴더로 재구성 + 결과 문서 작성

**결정**: `spain_euro2024/`에 있던 패스 네트워크 관련 노트북(`04_spain_euro2024_buildup.ipynb`)과 모든 산출물(`processed/` 전체 - 결승전 샘플 md/png/pdf, 결승전 포지션 기반 샘플, 교체 구간별 실험 이미지, 7경기 루프 결과)을 새로 만든 `spain_euro2024/pass_network/` 하위 폴더(노트북 + `processed/`)로 이동했다. 7경기 결과를 종합한 `pass_network/RESULTS.md`를 새로 작성했고, 노트북·PLAN.md·`ideas/backlog.md`·`scripts/test_position_pass_network.py`·`.claude/rules/analysis-workflow.md`의 경로 참조를 모두 갱신했다. `analysis-workflow.md`에는 "한 주제 안에 방법론이 여러 갈래면 방법론별 하위 폴더로 나눌 수 있다"는 규칙을 추가했다.
**이유**: "2024 유로 스페인의 빌드업 패턴" 주제는 애초 기획부터 방법론이 두 갈래(패스 네트워크 + 구역 기반 전진 경로)였다. 패스 네트워크 쪽 산출물이 늘어나면서(7경기 PNG, 여러 실험 이미지) `spain_euro2024/processed/`에 다 함께 쌓아두면 앞으로 시작할 구역 기반 전진 경로 산출물과 섞여 구분하기 어려워질 것으로 판단했다. 방법론별 하위 폴더(주제 폴더 안에 `PLAN.md`+노트북+`processed/`+`RESULTS.md` 패턴을 재귀적으로 적용)로 나눠, 상위 `PLAN.md`는 전체 기획을, 각 하위 폴더의 `RESULTS.md`는 해당 방법론의 종합 결과를 담당하도록 역할을 분리했다. 이 결과 문서 작성은 `PLAN.md` 진행 상황의 "7경기 종합 비교 및 결론 정리" 항목도 함께 완료시켰다.

## 2026-08-23 구역 기반 전진 경로: mplsoccer 표준 그리드 + 단일 피치 통합 시각화로 확정, 7경기 실행

**결정**: `spain_euro2024/` "구역 기반 전진 경로" 방법론을 다음과 같이 확정하고 7경기 전체를 실행했다.
- 구역 그리드: 처음엔 직접 정한 3등분×5채널(15구역)로 시작했으나, mplsoccer의 `Pitch(positional=True)`가 그리는 표준 Juego de Posición(포지션 플레이) 그리드(30구역, `pitch.dim.positional_x`/`positional_y`)로 교체했다. 하드코딩 대신 라이브러리 값을 그대로 읽어 쓴다.
- 시각화: 처음엔 화살표 피치와 별도 `imshow` 히트맵 패널을 나란히 뒀으나, 두 좌표계가 어긋나 보인다는 지적을 받아 피치 하나 위에 통합했다 - 30구역 점유(패스 시작 횟수, 방향 무관)를 배경 음영으로, 구역 간 전진 패스 전환(더 앞선 가로단으로 넘어간 성공 패스, `min_transition_count` 이상 반복된 쌍만)을 화살표로 겹쳐 그린다.
- `plot_zone_progression()`을 `src/visualizer.py`로 승격하고 `spain_euro2024/zone_progression/05_spain_euro2024_zone_progression.ipynb`로 7경기 전체를 실행, 결과는 `zone_progression/processed/spain_euro2024_zone_progression/`에 PNG 7장으로 저장했다.
- 노트북을 `jupyter nbconvert`/`nbclient`로 실행하다가 `sys.stdout.reconfigure(encoding='utf-8')`가 ipykernel의 `OutStream`에는 없는 메서드라 `AttributeError`가 나는 걸 발견해, `hasattr(sys.stdout, 'reconfigure')` 가드를 추가했다. 실행 후 노트북에 인라인 이미지가 임베드돼 740KB로 커진 것을, 기존 `pass_network` 노트북처럼 출력을 지워 7.5KB로 되돌렸다(이미지는 `processed/`의 PNG로 이미 저장돼 있으므로 노트북엔 코드만 남긴다).
**이유**: 사용자가 "요즘 축구는 포지션 플레이 기반 전술이 많은데 이를 반영할 수 없냐"고 제안했고, mplsoccer에 이미 이 이론(spielverlagerung.com에서 정의한 Juego de Posición)을 구현한 내장 그리드가 있다는 걸 사용자가 먼저 알려줬다 - 직접 만든 15구역보다 실제 코칭 현장에서 쓰는 표준 그리드를 쓰는 게 정확하고, 실제로 화면 겹침도 줄어드는 효과가 있었다(구역이 세밀해질수록 `min_transition_count` 문턱을 넘는 구역 쌍이 줄어듦). 이어서 사용자가 "화살표(피치 좌표)와 히트맵(별도 격자)의 방향이 안 맞아 보인다"고 지적해, 두 레이어를 하나의 피치 좌표계로 합쳐 원천적으로 해결했다.

## 2026-08-23 "2024 유로 스페인의 빌드업 패턴" 주제 완료 - 두 방법론 종합 결론 정리

**결정**: `spain_euro2024/zone_progression/RESULTS.md`(7경기 종합 결과)를 작성하고, 이어서 패스 네트워크와 구역 기반 전진 경로 두 방법론을 종합한 최종 결론을 상위 `spain_euro2024/PLAN.md`에 "종합 결론" 절로 정리했다 - "센터백을 넓게 벌리고 로드리 축의 중앙 순환으로 빌드업을 시작해 양쪽 풀백을 윙까지 전진시키는 구조(좌우 대칭)이지만, 실제 전진의 종착점(`Att-Mid/Left Wide` 구역)은 7경기 내내 일관되게 왼쪽으로 쏠렸다"는 결론이다. `ideas/backlog.md`의 해당 항목을 `진행중` → `완료`로 옮겼다.
**이유**: 사용자가 화살표 개수가 경기마다 다른 이유(표본 크기 차이)를 물어봐서 `RESULTS.md`의 "한계/유의사항"에 이를 명시적으로 설명하는 절을 넣었고, 이어서 두 방법론을 종합해달라고 요청했다. 두 방법론을 따로 보면 각자 부분적인 그림만 준다 - 패스 네트워크는 "구조"(좌우 대칭)를, 구역 기반 전진 경로는 "방향"(왼쪽 쏠림)을 보여주는데, 이 둘을 합쳐야만 "구조는 대칭이지만 실제 활용은 비대칭"이라는, 애초에 이 주제를 두 방법론으로 나눠 시작한 목적(분석 질문 4)에 부합하는 결론이 나왔다.

## 2026-08-23 종합 결론을 `spain_euro2024/RESULTS.md`로 분리

**결정**: 두 방법론(패스 네트워크/구역 기반 전진 경로)을 종합한 최종 결론을 `PLAN.md` 안의 절로 두지 않고, `pass_network/RESULTS.md`·`zone_progression/RESULTS.md`와 같은 레벨로 `spain_euro2024/RESULTS.md`를 새로 만들어 옮겼다. `PLAN.md`에는 결론 한 문장 요약 + 링크만 남겼고, 상단 폴더 안내와 `ideas/backlog.md` 링크도 이 문서를 가리키도록 갱신했다.
**이유**: 사용자가 "종합 결론에 대한 문서도 별도로 생성하는 게 어떨까"라고 제안했다. `PLAN.md`는 기획 문서(무엇을 왜 하는지)이고 결론은 결과 문서에 속하는데, 두 방법론 각각 이미 `RESULTS.md`가 있는 폴더 구조에서 최상위 종합 결론만 `PLAN.md`에 섞여 있는 게 비일관적이었다. 최상위에도 `RESULTS.md`를 둬서 "기획은 PLAN.md, 결과는 RESULTS.md"라는 패턴을 모든 레벨(주제 전체/방법론별)에서 동일하게 유지했다.

## 2026-08-25 "2024 유로 스페인의 빌드업 패턴" 심화 착수 - 기존 주제 폴더에 방법론 하위 폴더 2개 추가

**결정**: 완료 상태였던 "2024 유로 스페인의 빌드업 패턴"을 새 주제로 분리하지 않고, 기존 `spain_euro2024/` 주제 폴더 안에 방법론 하위 폴더 2개(`asymmetry_stats/`, `possession_chains/`)를 착수 예정 상태로 추가해 심화하기로 했다. `ideas/backlog.md`의 해당 항목을 `완료` → `진행중`으로 되돌리고(기존 완료 내용은 그대로 두고 "진행중" 절에 심화 항목을 새로 추가), `PLAN.md`에 분석 질문 5(좌우 비대칭 통계 검증)·6(possession 체인 추적)과 방법론 요약 스텁, 진행 상황 체크리스트를 추가했다.
- 분석 질문 5(`asymmetry_stats/`): 패스 네트워크의 "구조 유지"·구역 기반 전진 경로의 "왼쪽 쏠림" 판단이 지금까지 7장 이미지 정성 비교에 그쳤던 것을 수치·통계 검정으로 보완한다.
- 분석 질문 6(`possession_chains/`): 구역 기반 전진 경로가 개별 성공 패스 단위로만 화살표를 그려 "연속된 빌드업 시퀀스"를 못 잡던 한계를, StatsBomb `possession` id로 체인을 묶어 보완한다.
- 두 항목 모두 아직 데이터 검토 전 단계이며, 워크플로우대로 데이터 검토 → 방법론 확정 순으로 진행할 예정이다.
**이유**: 사용자가 완료된 분석의 한계(정성적 판단에 그침, 개별 패스 단위 집계)를 듣고 "이 분석을 더 깊게 가는 쪽"으로 다음 작업을 잡고 싶어했다. 같은 팀·같은 대회·같은 근본 질문을 더 깊게 검증하는 작업이라 별도 주제로 빼면 기존 결론과의 연결이 끊기고 백로그에 사실상 같은 주제가 중복되므로, `analysis-workflow.md`가 이미 지원하는 "한 주제 안에 방법론이 여러 갈래면 하위 폴더로 나눈다" 패턴을 재사용해 기존 `pass_network/`·`zone_progression/` 옆에 새 하위 폴더를 추가하는 쪽을 사용자와 상의해 택했다.

## 2026-08-26 possession 체인 추적 방법론 확정 및 7경기 실행

**결정**: `plot_possession_chain_progression()`을 `src/visualizer.py`에 추가하고 `spain_euro2024/possession_chains/`(노트북 + `processed/` + `RESULTS.md`)를 새로 만들어 7경기 전체를 실행했다.
- **체인 정의**: `type == 'Pass'`, `team == possession_team == '스페인'`, 성공 조건으로 걸러낸 뒤, 같은 `possession` id 안에 이 조건을 만족하는 패스가 `min_chain_length`(기본 2)회 이상인 possession만 "체인"으로 인정한다.
- **시각화 형식**: 여러 대안(Sankey류 다단계 흐름도, 대표 경로 Top-N)을 논의한 끝에, 메인 패널은 `plot_zone_progression()`과 동일한 30구역 점유 히트맵 + 전진 화살표를 재사용하되 체인 필터링된 패스만 집계하고, 오른쪽에 왼쪽/오른쪽으로 끝난 체인 중 가장 긴 것을 하나씩 뽑아 실제 좌표 그대로 이은 미니 패널 2개를 추가하는 형태로 확정했다. 처음엔 대표 체인을 메인 히트맵 위에 그대로 겹쳐 그렸으나, 왔다갔다하는 패스가 많은 체인(결승전 32패스짜리 왼쪽 체인)은 선이 피치를 가로질러 기존 화살표와 뒤섞여 못 알아볼 정도로 지저분해져, 겹치지 않는 별도 미니 피치로 분리했다.
- **결과**: 7경기 중 6경기에서 왼쪽으로 끝난 체인의 중앙값 패스 수가 오른쪽보다 많았다(예외: R16 조지아전, 스페인이 4-1로 압도하며 나온 51패스짜리 오른쪽 특이 체인 하나의 영향). `zone_progression`에서 확인한 "왼쪽 구역 점유 우세"를 "왼쪽 진출은 더 많은 패스를 거쳐 정교하게 조립된다"는 방향으로 구체화했다. 상세는 `spain_euro2024/possession_chains/RESULTS.md` 참고.
- **환경 이슈**: 이 세션의 샌드박스 bash/PowerShell 환경에서 `jupyter`/`jupyter-nbconvert` CLI(엔트리포인트 실행 파일 및 `python -m jupyter`)가 아무 출력 없이 항상 종료코드 1로 실패하는 문제를 발견했다 - `jupyter_core.command.main()`의 서브커맨드 디스패치 단계에서 조용히 실패하는 것으로 보인다. 우회책으로 `nbconvert.nbconvertapp.main()`을 Python에서 직접 호출해 노트북을 실행했다(`sys.argv`를 CLI 인자처럼 구성). 또한 직접 작성한 노트북 JSON의 셀 `id`를 빈 문자열로 두면 nbformat 검증에서 실패한다는 것도 확인해, 노트북을 코드로 생성할 때는 `uuid4().hex[:8]` 같은 고유 id를 반드시 채워야 한다.
**이유**: 사용자가 possession 체인 추적을 먼저 이어가기로 선택했고, 시각화 형식은 "구역 흐름 Sankey/화살표 다이어그램" 방향으로 시작해 실제 프로토타입 결과(대표 체인이 메인 히트맵과 겹쳐 지저분해짐)를 함께 보고 "별도 작은 패널로 분리"하는 쪽으로 반복 조정했다 - 워크플로우가 요구하는 "프로토타입 → 사용자와 함께 검증 → 확정" 절차를 그대로 따른 결과다.

## 2026-08-26 좌우 비대칭 통계 검증 완료 - "구조는 대칭" 결론을 통계로 재검증하고 수정

**결정**: 데이터 검토(`scripts/review_asymmetry_stats_data.py`, 7경기 전체) 후 `spain_euro2024/asymmetry_stats/`(노트북 + `processed/` + `RESULTS.md`)를 만들어 통계 검정을 실행했다.
- **지표 3개**: ① 구역 점유(전체 구역) 왼쪽 비율, ② 구역 점유(Att-Mid 가로단만) 왼쪽 비율, ③ 패스 네트워크(7경기 공통 4개 역할군 Back/Center Back/Defensive Midfield/Wing의 슬롯별 패스 수) 왼쪽 비율. 매치별로 계산해 완전 대칭(0.5)과의 차이를 `scipy.stats.ttest_1samp`(paired t-test, n=7)로 검정했다.
- **결과**: 세 지표 모두 p<0.05로 통계적으로 유의미하게 왼쪽으로 치우쳤다(구역 전체 p=0.0059, Att-Mid만 p=0.0370, 패스 네트워크 p=0.0315). 다만 효과 크기가 다르다 - 패스 네트워크(선수 관여도)는 평균 53.5%로 대칭에 가깝고, 구역 점유(목적지)는 56.9~59.7%로 더 크게 쏠렸다.
- **기존 결론 수정**: 이전까지 `spain_euro2024/RESULTS.md`는 "패스 네트워크 구조는 좌우 대칭, 구역 기반 전진 경로 방향만 비대칭"이라고 정성적으로 결론 내렸었다. 통계 검정 결과 이는 엄밀히는 부정확했다 - 패스 네트워크도 미세하지만 유의미하게 왼쪽으로 기울어 있었다. 상위 `RESULTS.md`·`PLAN.md`의 종합 결론을 "구조는 거의 대칭이지만 완벽하지는 않고, 그 작은 구조적 쏠림이 최종 전진 구역에서 크게 증폭된다"는 방향으로 수정했다. `ideas/backlog.md`도 함께 갱신해 "2024 유로 스페인의 빌드업 패턴" 항목을 심화 내용까지 포함해 완전히 완료 처리했다.
**이유**: 사용자가 좌우 비대칭 통계 검증부터 시작해달라고 요청했고, 데이터 검토 결과(구역 기준 좌/우 표본 132~370개, position 라벨 좌우 완벽 대응)를 보고 검정 방법(paired t-test, 매치별 왼쪽 비율)과 범위(구역 비율 검증 + 패스 네트워크 구조 검증 둘 다)를 함께 결정했다. 실제 검정 결과가 기존의 정성적 결론과 미묘하게 어긋나는 지점(패스 네트워크도 완전 대칭은 아님)을 발견해, 이를 숨기지 않고 상위 종합 결론에 반영해 더 정확한 결론으로 갱신했다.

## 2026-08-26 종합 결과를 블로그 발행용 문서(BLOG_POST.md)로 재구성, 이 과정에서 "왼쪽 쏠림의 이유" 분석 추가

**결정**: 사용자가 지금까지의 분석 결과를 자신의 블로그에 올릴 마크다운 문서로 정리해달라고 요청해 `spain_euro2024/BLOG_POST.md`를 작성했다(발행 전 이미지 상대 경로를 직접 교체해야 한다는 안내를 문서 상단 주석으로 남김). 첫 초안 제목("스페인은 왜 항상 왼쪽으로 공격했을까")에 대해 사용자가 "본문은 원인이 아니라 결과(왼쪽에서 공격이 잦았다는 사실)만 다룬다"고 지적했다.
- 이 지적을 받아들여 새 심화 하위 폴더 `spain_euro2024/chain_outcomes/`를 만들고, 두 가설을 데이터로 검증했다 - ① 왼쪽 체인이 오른쪽보다 득점에 더 효율적이었는가(Shot 이벤트를 `possession_chains`의 체인 정의와 연결), ② 왼쪽 포지션 조합이 더 자주 활용됐는가(패스 네트워크 슬롯 쌍을 L-L/R-R로 나눠 비교).
- **결과는 처음 가설과 달랐다**: 득점 효율은 원인이 아니었다(왼쪽 18.8% vs 오른쪽 17.2% 슈팅 전환율로 비슷했고, 오히려 중앙으로 끝난 체인이 46.5%로 압도적으로 효율적이었다 - 7경기 골 14개 중 10개가 중앙 체인에서 나옴). 대신 선수 조합 밀도가 통계적으로 뒷받침되는 원인이었다(왼쪽 슬롯끼리의 패스 연결 비율 57.2%, paired t-test p=0.0080, 7경기 전부에서 왼쪽 우세).
- 이 발견을 `PLAN.md`·`RESULTS.md`·`ideas/backlog.md`에 반영해 종합 결론을 갱신했고, `BLOG_POST.md`도 "왜"에 대한 답(득점 효율 아님, 선수 조합 밀도임)을 담아 다시 썼다.
**이유**: "정말 그 이유에 대해서도 다룬다면"이라는 사용자 피드백이 구체적인 가설(득점 효율, 볼 점유, 선수 개인 특성)을 제시했다. 이 중 이벤트 데이터로 가장 엄밀하게 검증 가능한 두 가지(득점 효율, 조합 빈도)를 선택해 실제로 검증했고, 결과가 처음 예상(왼쪽이 더 효율적이었을 것)과 반대로 나왔다 - 가설을 확인하는 데 그치지 않고 데이터가 실제로 말해주는 것을 그대로 보고하는 것을 이 프로젝트의 원칙으로 삼았다(기존 좌우 비대칭 통계 검증 때도 같은 태도를 유지했다).

## 2026-08-26 "왼쪽 쏠림의 이유"에 세 번째 가설(크로스/어시스트 위치) 추가

**결정**: 사용자가 chain_outcomes 결과를 보고 "가장 많이 어시스트를 기록한 지역이 왼쪽일 수도, 컷백/크로스가 왼쪽에서 올라와서 득점했을 수도 있다"는 후속 가설을 제시해, `chain_outcomes/`에 세 번째 검증을 추가했다. StatsBomb의 `pass_goal_assist`(골 직결)·`pass_shot_assist`(슈팅 직결)·`pass_cross`(크로스) 플래그가 참인 패스의 시작 위치를 왼쪽/중앙/오른쪽으로 분류해 7경기 합산 집계 + `asymmetry_stats`와 동일한 paired t-test로 검정했다.
- **크로스는 통계적으로 유의미하게 왼쪽에서 더 많이 올라왔다**(평균 60.5%, p=0.0114, 7경기 중 6경기 왼쪽 우세) - 기존 선수 조합 밀도 결과(L-L 57.2%)와 맥락이 통한다.
- **그러나 골 어시스트(오른쪽이 근소 우세, 5 vs 4, 표본 12개라 검정 생략)와 슈팅 어시스트(왼쪽 53.9%, p=0.6458로 유의하지 않음)는 좌우 차이가 뚜렷하지 않았다.** 즉 왼쪽에서 크로스는 더 많이 올라왔지만, 그게 실제 득점 기여로 유의미하게 더 잘 이어지진 않았다 - 이미 확인한 "득점 효율은 중앙이 압도적" 결과와 일관된다.
- `08_spain_euro2024_chain_outcomes.ipynb`에 3번째 패널(크로스 좌/우 비율 산점도)을 추가해 재실행했고, `chain_outcomes/RESULTS.md`·`PLAN.md`·`RESULTS.md`·`ideas/backlog.md`·`BLOG_POST.md`를 모두 갱신했다.
**이유**: 사용자가 "왼쪽이 더 효율적이었다"는 가설을 여러 각도(득점 패턴, 어시스트 지역, 박스 근처 전술 빈도)로 구체화해 제시했다. 이미 검증한 "체인 전체의 슈팅 효율"만으로는 크로스처럼 특정 패스 유형에 집중된 신호를 놓칠 수 있어, StatsBomb이 제공하는 어시스트/크로스 플래그로 더 세밀하게 재검증했다. 이번에도 결과는 부분적으로만 가설을 지지했다(크로스는 유의, 어시스트는 비유의) - 활용 빈도(조합·크로스)와 최종 득점 기여(어시스트·골)가 서로 다른 문제라는 것을 데이터로 명확히 구분해 보여준 사례다.

## 2026-08-31 블로그 발행용 문서를 최상위 `blog/` 폴더로 분리

**결정**: 티스토리 계정 생성 이후 블로그 발행 워크플로우를 준비하면서, `spain_euro2024/` 안에 있던 `BLOG_POST.md`·`BLOG_POST.html`(+ 로컬 전용 `BLOG_POST.pdf`)을 새로 만든 최상위 `blog/spain_euro2024/` 폴더로 옮겼다. 분석 주제 폴더(`spain_euro2024/`)와 같은 이름의 하위 폴더를 `blog/` 아래에 둬서 어느 분석의 발행용 문서인지 바로 대응되게 했다. 이미지는 옮기지 않고 원래 위치(`spain_euro2024/*/processed/`)에 그대로 두었으며, `BLOG_POST.md`/`BLOG_POST.html`의 이미지 상대 경로를 새 위치 기준(`../../spain_euro2024/...`)으로 고쳤다. `spain_euro2024/PLAN.md`·`ideas/backlog.md`의 `BLOG_POST.md` 링크도 새 경로로 갱신했고, `CLAUDE.md` 아키텍처 섹션에 `blog/` 폴더의 역할(발행용 원고 전용, 이미지는 원본 위치를 상대 경로로만 참조)을 추가했다.
**이유**: 사용자가 "블로그 글 문서도 계속 폴더로 관리하고 싶다"며, 분석 산출물(주제 폴더)과 블로그 발행 산출물을 별도 최상위 폴더로 분리해달라고 요청했다. 앞으로 다른 주제도 블로그로 발행할 가능성이 있으므로, `blog/<주제 폴더명>/` 패턴을 표준으로 정해 향후 주제에도 그대로 반복 적용할 수 있게 했다. 이미지를 옮기거나 복제하지 않고 상대 경로만 재계산한 이유는, 분석 산출물의 원본 위치는 여전히 해당 주제 폴더의 `processed/`여야 한다는 기존 규칙(`.claude/rules/analysis-workflow.md`)과 일관성을 유지하기 위함이다.

## 2026-08-31 Spain Euro 2024 블로그 글을 티스토리에 직접 발행, "이미지 URL 추출" 방식 폐기

**결정**: `2024 유로 스페인은 왜 항상 왼쪽으로 공격했을까` 글을 티스토리(tj-archive)에 공개 발행했다 → https://tj-archive.tistory.com/1 (글 번호 1, 카테고리 없음).
- **발행 방식**: 브라우저 자동화로 티스토리 글쓰기 에디터(TinyMCE)에 이미지 4개를 업로드하고, `tinymce.activeEditor.setContent()`로 본문 HTML(BLOG_POST 내용) + 4개 이미지 `<figure>`를 한 번에 주입한 뒤 "공개 발행"했다. HTML 붙여넣기 모드는 쓰지 않았다.
- **이전 세션 방식이 틀렸음을 확인**: 직전 세션은 "이미지를 임시 초안에 올려 kakaocdn URL만 빼내 BLOG_POST.md/html에 하드코딩" 했는데, 그 URL 4개가 전부 404였다. 원인 - **티스토리 이미지 URL은 `credential`·`expires`(약 6시간)·`signature` 파라미터가 붙은 만료형 서명 URL이고, 글이 열릴 때마다 서버가 새로 서명한다.** 쿼리스트링을 뗀 `blog.kakaocdn.net/dna/.../img.png`는 발행 후에도 404. 즉 외부에서 재사용 가능한 영구 이미지 URL이 존재하지 않는다.
- **BLOG_POST.md/html 처리**: 죽은 kakaocdn URL 4개를 저장소 원본(`../../spain_euro2024/*/processed/*.png`) 상대 경로로 되돌렸다. 이 두 파일은 이제 "발행용 원고 + 발행 후 보관용 사본" 역할이며, 상단 주석에 발행 URL과 "티스토리 이미지는 핫링크 불가" 사실을 적었다.
**이유**: 사용자가 "이미지 업로드하고 URL 추출하다 세션이 끊겼다"며 이어서 해달라고 요청했고, 재업로드 방식(브라우저로 새 초안)을 택했다. 진행 중 티스토리 이미지 URL이 만료형 서명 URL이라 애초에 하드코딩이 불가능하다는 걸 확인해, 사용자와 상의 후 "본문까지 채워 공개 발행" 쪽으로 전환했다. 향후 다른 주제도 같은 방식(에디터에 직접 주입 → 발행)으로 올리고, BLOG_POST.* 의 이미지는 로컬 상대 경로로 유지한다.

## 2026-08-31 Spain Euro 2024 블로그 글을 데이터 저널리즘 스타일 HTML로 재디자인, #1 재발행

**결정**: 사용자가 "엠대쉬 → 하이픈 치환 + 전문 아티클처럼 HTML로 꾸미기"를 요청해, `blog/spain_euro2024/BLOG_POST.html`을 데이터 저널리즘 톤(The Athletic 계열)으로 다시 쓰고 티스토리 글 #1을 그 내용으로 재발행했다.
- **엠대쉬 치환**: `BLOG_POST.md`·`BLOG_POST.html` 본문 + 티스토리 제목의 U+2014를 전부 하이픈(-)으로 바꿨다. 문장 흐름상 대쉬가 어색한 자리는 마침표로 끊어 다듬었다.
- **디자인**: 인라인 style만 사용(티스토리 기본모드 TinyMCE가 class CSS를 못 살림 - `tinymce.activeEditor.setContent()` 라운드트립으로 div/table/td/th의 background·border·border-left·border-radius·padding이 보존되는 것 확인). 구성 요소 - 상단 kicker(빨강 소제목), 리드 문단, "핵심 요약" 콜아웃 박스(빨강 좌측 보더), 핵심 수치 4개 스탯 스트립, 번호 붙은 섹션 헤딩(빨강 번호 + 검정 밑줄), 검정 헤더 행 + 우측 정렬 숫자의 표(핵심 행은 연분홍 배경 + 빨강 볼드), 회색 "분석의 한계" 박스, 출처 푸터. 액센트 컬러 `#c0392b`.
- **이미지**: 에디터에 이미 업로드된 4개 `<figure>`를 DOM에서 캡처(이미지 코드로 매칭: 3E8bx=패스네트워크, ch9ln5=구역전진, PRj1K=비대칭, V93Bs=체인결과), figcaption만 한국어 "그림 N." 캡션으로 교체해 새 본문 HTML에 끼워넣었다. `BLOG_POST.html`의 `<img>`는 로컬 상대 경로 유지.
- **발행 방식**: `manage/newpost/1` 편집 화면에서 기본모드 유지 → `setContent()` → 제목 필드 하이픈 치환 → "공개 발행". 발행 후 라이브 페이지에서 인라인 스타일(콜아웃/스탯/표/헤딩)이 모두 정상 렌더링됨을 확인.
**이유**: 사용자가 데이터 저널리즘 방향과 #1 업데이트(새 글 아님)를 명시적으로 선택했다. 티스토리 HTML 모드로 전환하면 되돌리기 어렵고 이미지 재서명이 불확실해, 이미 검증된 "기본모드 + setContent + 인라인 스타일" 경로를 유지했다.

## 2026-08-31 워크플로우 일부 단계를 서브에이전트로 분담, 전 단계 오케스트레이션은 채택 안 함

**결정**: 사용자가 "각 단계를 서브에이전트로 돌릴 수 있지 않냐"고 제안해, 논의 끝에 **격리 가능한 3개 작업만** `.claude/agents/`에 정의했다.
- `data-review` - 2단계(데이터 검토): 방법론 스펙 + 대상 경기를 받아 결측률·분포·이벤트 빈도를 확인하고 `scripts/review_<주제>_data.py` + 리포트 산출. `PLAN.md`·`statsbomb-data-notes.md`는 직접 안 고치고 문장 제안만.
- `analysis-verifier` - 완료 전 검증 스윕(읽기 전용): `RESULTS.md` 수치 일치, 진행 상태 최신성, `CLAUDE.md` ↔ `GEMINI.md` 동기화, 엠대시/엔대시 린트, 이미지 상대경로 유효성 점검 후 불일치 목록 보고.
- `blog-draft` - `RESULTS.md`들을 종합해 `blog/<주제>/BLOG_POST.md` + `.html` 초안 생성(데이터 저널리즘 톤, 하이픈만, 티스토리 TinyMCE 호환 인라인 스타일). 발행 안 함.
- `.claude/rules/analysis-workflow.md`에 "서브에이전트 분담" 절, `CLAUDE.md`·`GEMINI.md` 아키텍처/워크플로우 섹션에 참조를 추가했다(GEMINI 쪽은 "Claude Code 전용, Gemini는 직접 수행하되 정의를 규격 참고용으로"로 표기).
- **메인 세션 유지**: 1단계 아이디어 논의(사용자와의 대화라 위임 불가), `PLAN.md` 방법론 확정, 분석 실행 + 함수 이터레이션, 산출물 리뷰, `DECISIONS.md`·`PLAN.md`·`backlog.md` 갱신.
**이유**: "메인 에이전트가 각 서브에이전트에 전 단계를 명령"하는 오케스트레이터 방식도 가능했으나, ① 1단계가 대화라 애초에 위임 불가, ② 서브에이전트는 cold-start마다 `DECISIONS.md`·`PLAN.md`·rules를 다시 읽어 단계 수만큼 비용이 곱해짐, ③ 3~5단계는 산출물을 보며 함수·방법론을 고치는 왕복이 잦아 별도 cold 에이전트로 쪼개면 판단이 중요한 지점에서 맥락이 샘, ④ 브리핑 작성이 그 단계를 직접 하는 것보다 품이 더 들 수 있음. 반면 데이터 검토·검증 스윕·블로그 초안은 입력(`PLAN.md`/`RESULTS.md`)과 출력(리포트/초안 파일)이 깔끔해 위임 이득이 크고, 핵심 이득은 자동화보다 컨텍스트 격리(dataframe 덤프·grep 출력을 메인에서 치움)다.

## 2026-08-31 블로그 발행 전 검토에 AI 티 윤문 단계 추가

**결정**: 블로그 발행 워크플로우에 "AI 티 윤문" 단계를 명시했다. `blog-draft` 초안 → ① 본문 수치·결론을 `RESULTS.md`와 대조 → ② 제목·프레이밍 사용자 확정 → ③ `humanize-korean` 스킬(`/humanize-korean`)로 확정 본문을 윤문(번역투·영어 인용 과다·기계적 병렬·관용구/불릿/이모지 과다 등 AI 티 패턴 제거) → ④ 티스토리 직접 주입 발행. 윤문 결과는 `BLOG_POST.md`·`BLOG_POST.html` 양쪽에 반영하고, 윤문 전후로 수치·고유명사 불변을 재확인한다. `.claude/rules/analysis-workflow.md`에 "블로그 발행 전 검토" 절을 추가하고, `blog-draft.md`의 "하지 않는 것"에 "윤문은 직접 안 함(메인 세션이 별도로)"를 명시했으며, `CLAUDE.md`·`GEMINI.md`에도 반영했다.
**이유**: 사용자가 블로그 글 검토 과정에 윤문을 넣어달라고 요청했다. `humanize-korean` 스킬은 파이프라인에 content-fidelity 감사가 있어 내용·수치·인용을 바꾸지 않으므로, 수치가 핵심인 데이터 저널리즘 글에도 안전하게 적용할 수 있다. 윤문을 `blog-draft` 서브에이전트 안에 넣지 않은 이유는 그 스킬이 자체적으로 서브에이전트를 띄우는 오케스트레이터라 도구가 제한된 서브에이전트 안에서 돌릴 수 없고, 제목·프레이밍이 확정된 뒤에 실행해야 하기 때문이다. 윤문 순서를 마지막(발행 직전)에 둔 것은, 프레이밍이 계속 바뀌는 동안 윤문하면 헛일이 되기 때문이다.

## 2026-08-31 `data-review` 에이전트를 판단/실행으로 쪼개 실행 부분만 Haiku로 (`data-review-run`)

**결정**: 사용자가 "data-review를 세부 작업으로 나눠 Haiku를 최대한 활용"하길 원해, 2단계(데이터 검토)를 세 조각으로 나눴다.
- **체크리스트 스펙 작성 (메인 세션, Sonnet)**: 방법론이 어떤 가정에 기대는지 짚어, 확인할 항목을 `{df 필터 / 컬럼·이벤트 / 연산 / 경기별|전체 / 자동 플래그 임계값}` 형태로 구체적으로 못 박는다.
- **전사·실행 (`data-review-run`, Haiku)**: 스펙 + 템플릿 스크립트를 받아 `scripts/review_<주제>_data.py`를 작성·실행하고 경기별 표 + 자동 플래그를 돌려준다. 체크리스트에 없는 항목 추가 금지, 모호하면 추측 말고 보고, 해석·문서 편집 안 함.
- **검산 + 해석 + 기록 (메인 세션, Sonnet)**: 돌아온 표를 눈대중 검산한 뒤(필터 오류를 여기서 잡음) 표본 충분성·방법론 수정 필요 여부를 판단하고 `PLAN.md`/`statsbomb-data-notes.md`에 반영한다.
- 기존 `data-review.md`(Sonnet, 판단+실행 통합)를 삭제하고 `data-review-run.md`(`model: haiku`)로 대체했다. `.claude/rules/analysis-workflow.md`의 "2단계 - 데이터 검토"에 "실행 분담" 하위 절을, "서브에이전트 분담" 절에 모델 지정 근거를 추가했고 `CLAUDE.md`·`GEMINI.md`도 갱신했다. 간단한 확인(컬럼 1~2개 결측률)은 위임 없이 메인 세션이 직접 한다.
**이유**: `data-review`가 하던 일 중 "무엇을 확인할지 정하기"와 "결과 해석"만 판단이고, "체크리스트를 스크립트로 옮겨 실행하기"는 스펙이 구체적이면 창의성 없는 전사다. 이 전사·실행 부분이 토큰을 가장 많이 쓰므로(스크립트 이터레이션, `value_counts` 덤프) 여기를 Haiku 컨텍스트로 격리하면 비용을 줄이면서 메인 세션은 짧은 스펙과 압축된 표만 주고받는다. Sonnet 플래너 에이전트가 Haiku 러너를 다시 스폰하는 중첩 구조도 가능했으나, 중첩 서브에이전트는 불안정하고 메인 세션이 이미 Sonnet이라 이득이 없어 판단을 메인 세션으로 올렸다. Haiku 오작동 위험(필터 오류 등)은 스펙의 "모호하면 추측 금지" 규칙과 메인 세션의 눈대중 검산으로 막는다.

## 2026-09-01 `analysis-verifier`·`blog-draft`도 기계적 부분 분리 (`scripts/check_docs.py`, `blog-html`)

**결정**: 나머지 두 에이전트에서도 Haiku·스크립트로 뺄 수 있는 부분을 찾아 분리했다.
- **`analysis-verifier`**: 완전 결정론적인 세 검사(엠대시/엔대시 린트, `.md`·`.html` 이미지 상대경로 유효성, `CLAUDE.md` <-> `GEMINI.md` 공통 섹션 diff)를 새 스크립트 `scripts/check_docs.py`로 뺐다. `analysis-verifier`는 1단계로 이 스크립트를 돌리고, 2단계로 판단이 필요한 점검(문서 간 수치 모순, 진행 상태 최신성, blog ↔ 분석 일치)만 직접 한다. Haiku 서브에이전트를 만들지 않은 이유: "엠대시 있나?"는 `grep`이 LLM보다 엄격하고, 스크립트는 커밋 전 훅 등으로 재사용 가능하며 환각 위험이 0이다. CLI 이름 차이(Claude/Gemini)는 diff 전에 정규화해 의도된 분기가 노이즈로 뜨지 않게 했다.
- **`blog-draft` → `blog-draft`(원고) + `blog-html`(변환)**: `blog-draft`는 `BLOG_POST.md` 마크다운 원고만 쓴다. `BLOG_POST.html` 생성은 새 Haiku 에이전트 `blog-html`이 맡되, 본문 텍스트는 한 글자도 안 바꾸고 `blog/spain_euro2024/BLOG_POST.html`의 인라인 스타일 템플릿을 매핑 규칙표대로 적용하는 포맷 변환만 한다. 큐레이션이 필요한 부분(스탯 스트립에 넣을 수치, 표의 강조 행)은 `blog-draft`가 원고에 마커(`<!-- STAT-STRIP: ... -->`, 강조 행 첫 셀 볼드)로 지정해 Haiku가 판단하지 않게 했다. 실행 위치는 "블로그 발행 전 검토"의 윤문(3단계) 직후 4단계 - 윤문된 최종 MD로부터 HTML을 생성한다.
- `.claude/agents/`는 이제 `data-review-run`·`analysis-verifier`·`blog-draft`·`blog-html` 4개. `.claude/rules/analysis-workflow.md`, `CLAUDE.md`, `GEMINI.md`를 모두 갱신했다.
**이유**: 사용자가 세 에이전트 전부에서 Haiku 활용 여지를 살펴봐 달라고 했다. 살펴본 결과 `analysis-verifier`의 기계적 부분은 Haiku보다 결정론적 스크립트가 더 낫고(정확도·재사용성·비용 0), `blog-draft`는 "글을 쓰는 창작"과 "정해진 템플릿을 입히는 변환"이 깔끔하게 갈려 후자를 Haiku로 뗄 수 있었다. 산문 작성 자체는 스토리를 짜는 게 가치라 Haiku 요약이 밋밋해지므로 `blog-draft`(Sonnet)에 그대로 뒀다. `blog-html`의 오작동 위험(class CSS 오용, 변환 중 수치 훼손)은 명시적 매핑 규칙표 + `check_docs.py` 린트 + 자기 확인(MD/HTML 본문 일치) + 메인 세션 눈대중으로 막는다.

## 2026-08-31 문서에서 엠대시/엔대시 금지를 프로젝트 전역 규칙으로 승격, 기존 문서 일괄 치환

**결정**: 사용자가 "문서를 작성할 때 엠대시 대신 하이픈을 쓰도록 CLAUDE.md에 명시"해달라고 요청해, `CLAUDE.md`의 "참고 사항"에 엠대시("—", U+2014)·엔대시("–", U+2013)를 쓰지 말고 하이픈("-")을 쓰라는 항목을 추가했다(삽입구는 " - "나 쉼표·괄호로, 숫자 범위도 "0-18"처럼 하이픈으로). 이어서 저장소의 기존 문서·노트북·소스 전체를 일괄 치환했다.
- 치환 범위: `.md` 14개(`CLAUDE.md`, `DECISIONS.md`, `GEMINI.md`, `.claude/rules/*.md`, `ideas/backlog.md`, `spain_euro2024/**/*.md`), `.ipynb` 4개(마크다운 셀), `.py` 2개(`src/visualizer.py`, `scripts/test_zone_progression.py` 독스트링). `blog/`의 `BLOG_POST.*`는 2026-08-31 재디자인 때 이미 치환 완료라 대상 아님.
- 치환 규칙: " — "/" – "(공백 있는 삽입구)는 " - "로, 나머지 붙은 형태는 "-"로. `spain_euro2024/PLAN.md`의 숫자 범위 엔대시(`0–18` 등)도 하이픈으로.
- `CLAUDE.md` 규칙 문장 자체에는 어떤 문자를 금지하는지 보여주기 위해 리터럴 "—"/"–"를 U+2014/U+2013 라벨과 함께 남겨뒀다(저장소에서 유일하게 이 문자가 의도적으로 존재하는 위치).
**이유**: 직전 블로그 작업에서 엠대시를 하이픈으로 바꾸는 일이 반복됐고, 사용자가 이를 일회성이 아니라 프로젝트 표준으로 고정하길 원했다. 규칙만 추가하고 기존 문서를 놔두면 새 문서와 문체가 어긋나므로, 규칙 추가와 동시에 기존 자산 전체를 정리했다. 소스 코드 독스트링까지 포함한 이유는 한국어 문체 통일(`CLAUDE.md` 참고 사항의 기존 원칙)과 방향이 같기 때문이다.

## 2026-08-31 GEMINI.md 생성 및 CLAUDE.md-GEMINI.md 상호 동기화 규칙 추가

**결정**: Gemini / Antigravity CLI 환경을 지원하기 위해 `CLAUDE.md`를 기반으로 `GEMINI.md`를 생성했다. 또한 두 AI 도구가 일관된 프로젝트 컨텍스트와 규칙을 유지할 수 있도록, `CLAUDE.md`와 `GEMINI.md`의 "세션 시작 시 필독" 및 정기 검토 섹션에 두 파일 간의 동기화 상태를 매 세션마다 확인하고 유지하도록 명시했다.
**이유**: 사용자가 Claude Code와 Gemini / Antigravity CLI를 병행해 사용할 수 있는 환경에서, 한쪽 가이드에만 변경사항이 반영되면 AI 도구 간 동작 불일치가 발생할 수 있다. 이를 방지하기 위해 상호 동기화 규칙을 명시적으로 고정했다.

## 2026-09-01 블로그 발행 전 검토에 태그 선정 단계 추가 (질문증강 방식)

**결정**: 블로그 발행 워크플로우(`analysis-workflow.md`의 "블로그 발행 전 검토" 절)에 HTML 변환과 발행 사이 5단계로 "태그 선정"을 추가했다. 카테고리는 **선수/팀 · 전술(빌드업/세트피스/공격/수비/전개 등) · 데이터분석 · 축구 · 대회** 다섯 가지로 고정하고, 1단계 아이디어 논의와 같은 질문증강 방식 - 태그 후보를 곧장 나열하지 않고 카테고리별로 되묻는 질문을 던져 답변에 따라 좁혀가는 방식 - 으로 선정한다. 카테고리당 1~2개씩, 총 6~8개로 정리한다. `CLAUDE.md`·`GEMINI.md`의 블로그 발행 순서 요약 문장에도 "→ 태그 선정(질문증강 방식)"을 추가했다.
**이유**: 스페인 유로 2024 빌드업 글을 실제로 티스토리에서 편집하던 중 사용자가 태그 기능을 발견하고 추천을 요청했다. 처음엔 카테고리별로 태그를 바로 나열해 제안했는데, 사용자가 이를 선수/팀·전술·데이터분석·축구·대회 다섯 카테고리로 구조화하고 싶어했고, 앞으로도 계속 쓸 방식이라며 워크플로우 문서화를 요청했다. 태그 선정도 1단계 아이디어 논의와 마찬가지로 사용자의 의도(이 글이 어떤 선수/전술/대회에 방점을 찍는지)가 카테고리마다 다를 수 있어, 바로 답을 나열하기보다 질문증강 방식이 적합하다고 판단해 그 화법을 그대로 재사용했다.

## 2026-09-01 스페인 빌드업 글 재발행 (#1 삭제 → #2로 새로 발행)

**결정**: 사용자가 티스토리 글 #1(`https://tj-archive.tistory.com/1`)을 삭제하고, 윤문·핵심 요약 리스트화·태그 9개가 반영된 최신 본문을 새 글 `https://tj-archive.tistory.com/2`로 직접 발행했다. `blog/spain_euro2024/BLOG_POST.md`·`BLOG_POST.html` 상단 주석과 `ideas/backlog.md`의 발행 링크를 `/1` → `/2`로 갱신했다. 2026-08-31의 기존 `/1` 관련 항목(최초 발행·재디자인 기록)은 당시 사실을 그대로 보존하기 위해 수정하지 않았다.
**이유**: 이미 발행된 글 #1을 HTML 코드뷰에서 부분 수정(윤문 반영, 핵심 요약 리스트화, 이미지 재삽입)하려던 시도가 티스토리 HTML 코드뷰의 화면-내부상태 불일치 문제로 자동화가 막혔다(같은 날 앞선 대화 참고 - `textarea.value`를 스크립트로 바꿔도 화면에 반영되지 않고, 리로드 시 "저장 안 된 변경사항" 경고만 뜸). 사용자가 "글을 지우고 새로 작성해도 되냐"고 물었고, URL이 바뀐다는 점만 안내한 뒤 진행은 사용자에게 맡겼다. 사용자가 직접 티스토리 에디터에서 이미지 재업로드(`[##_Image|...]` 플레이스홀더 자동 생성 확인), 캡션·여백 스타일 수동 조정, 태그 9개(질문증강 방식으로 카테고리별 선정: 선수/팀·전술·데이터분석·축구·대회) 적용까지 모두 마치고 새 글로 발행했다.

## 2026-09-07 아이디어 논의·방법론 확정 단계는 메인 세션 모델을 Opus로 전환 권장

**결정**: `.claude/rules/analysis-workflow.md`의 1단계(아이디어 논의)와 "서브에이전트 분담" 절에, 메인 세션이 진행하는 판단 중심 단계 - 1단계 아이디어 논의와 `PLAN.md` 방법론 확정 - 는 `/model`로 Opus로 전환해 진행하고 분석 실행 단계부터 기본 Sonnet으로 되돌리는 것을 권장한다는 문구를 추가했다. 이번에는 사용자 요청으로 `GEMINI.md` 동기화는 생략했다(모델 티어링은 Claude Code 전용 내용이고, 규칙 본문은 두 CLI가 공유하는 `analysis-workflow.md`에만 넣어 `GEMINI.md` 본문에 따로 추가할 내용은 없다).
**이유**: 아이디어 논의는 "무엇을 되물을지" 판단이 핵심이고 대화가 몇 턴으로 짧아 토큰 부담이 작다 - Opus의 추론·도메인 감각 이점은 크고 비용 부담은 작아, Haiku로 뺀 전사·변환 작업과 정반대 성격의 단계다. 처음엔 Fable을 검토했으나 별도 크레딧이 필요해 Opus로 정했다. 작업별 모델 자동 전환은 하네스가 지원하지 않고(훅으로도 모델 교체 불가), 이 단계를 서브에이전트로 빼면 사용자와의 왕복 대화가 불가능하므로, 수동 `/model` 전환을 관례로 문서에 고정하는 방식을 택했다.

## 2026-09-08 아이디어 도출: "침투!" - 2022 카타르 한국의 침투 선택지 주제로 구체화

**결정**: `ideas/backlog.md`에 **"침투!" - 2022 카타르 한국, 전방에는 얼마나 있었나**를 `구체화` 상태로 기록했다. 팀이 전진하려는 순간 상대 수비 라인 뒤에서 패스를 받을 수 있는 위치에 선 자기 팀 선수 수("침투 선택지")를 StatsBomb **360 프리즈프레임**으로 세어, 32개 팀 조별리그 3경기 분포 위에 한국을 배치하고 그 지표가 대회 성적과 관계가 있는지 본다. 확장 질문으로 (a) 전진 상황의 양 x 상황당 선택지 밀도 2차원 비교(역습형 팀도 이기는가), (b) 한국의 16강 브라질전이 조별리그 3경기와 실제로 달랐는가를 둔다. 산출물에 `plot_freeze_frame()`(한 순간의 전 선수 좌표 스냅샷)을 포함하기로 했다.

**이유**: 출발점은 2026 북중미 월드컵 한국 대표팀의 부진과, 그때 해설자들이 반복한 "뛰어 들어가야 한다"·"후방 수비 숫자를 줄여야 한다"는 지적이 실제로 무엇을 보고 하는 말인지에 대한 사용자의 궁금증이었다. 오픈데이터에 2026 월드컵이 없어 그 대회 자체는 분석할 수 없지만, 질문의 본체가 "해설의 전술 언어가 데이터로 무엇인가"라 렌즈만 2022년 자료로 옮겼다. 두 지적 중 침투를 고른 것은 후방 잔류가 의도가 자명한 반면 침투는 그렇지 않기 때문이고, 마무리 국면(박스 채우기)이 아니라 전진 국면을 고른 것은 전진 국면을 보면 "앞이 막혀 크로스로 갔다"까지 함께 읽히지만 그 역은 성립하지 않기 때문이다. 이 프로젝트에서 360을 쓰는 것은 처음인데, 기존 분석이 전부 "공을 만진 선수"만 볼 수 있었던 반면 해설의 지적은 정확히 공 없는 선수에 대한 것이라 이 데이터가 필수다.

범위를 조별리그로 한정한 것은 대회 전체(64경기)를 쓰면 세 가지가 동시에 어긋나기 때문이다. (1) 조별 탈락 팀 3경기 vs 결승 진출 팀 7경기로 표본이 불균등해 분포의 극단이 소표본 팀으로 채워진다. (2) 멀리 간 팀일수록 성격이 다른 녹아웃 경기가 평균에 섞인다. (3) 결과로 쓸 "얼마나 멀리 갔나"와 재료인 "몇 경기를 뛰었나"가 같은 것이라 순환 논리가 된다. 조별리그 3경기로 통일하면 셋이 함께 풀리고, 부수 효과로 한국의 브라질전이 기준선 밖 별도 사례가 되어 확장 질문 (b)가 성립한다. 기준선에 약팀을 포함하는 것이 낭비라는 지적이 있었으나, 팀을 평균 하나로 뭉개지 않고 각각 점으로 남기면 약팀이 대조군 역할을 하므로 "이 지표가 성적과 관계있는가"라는 원래 질문에 답하려면 오히려 필수라고 판단했다.

## 2026-09-08 문서 정기 검토

**결정**: `CLAUDE.md`·`GEMINI.md`·`.claude/rules/*.md`를 다시 읽고 실제 저장소 구조와 대조했다. 아래를 두 파일에 동일하게 반영했다.

- **프로젝트 개요**: "이벤트/추적 데이터" → "이벤트 데이터 + 일부 대회는 360 프리즈프레임 포함". 오픈데이터에 실제 트래킹 데이터는 없고 360 프리즈프레임이 있으므로 표현을 정확히 했다. 시각화 예시에도 패스 네트워크·구역 전진 경로를 추가했다.
- **`src/visualizer.py`**: 스페인 주제에서 승격된 `plot_pass_network_by_position`, `plot_zone_progression`, `plot_possession_chain_progression` 세 함수가 문서에 빠져 있어 추가했다. `src/data_loader.py`에도 `get_match_lineups` 등 실제 공개 함수를 명시했다.
- **`scripts/`**: "예제 + 샌드박스"라는 서술이 실제와 어긋나 있었다. 지금은 `example_analysis.py`, `check_docs.py`, `review_<주제>_data.py`(2단계 산출물), `test_*.py`(승격 전 프로토타입) 네 갈래가 쌓이므로 그대로 적었다.
- **주제 폴더**: 방법론이 여러 갈래로 나뉘면 하위 폴더에 `RESULTS.md`를 둔다는 관례(`spain_euro2024/`는 다섯 갈래)가 `analysis-workflow.md`에만 있고 아키텍처 서술에 없어 한 문장 추가했다.
- **`blog/`·`_workspace/`**: 둘 다 `.gitignore` 대상이라는 사실을 명시하고, 문서에 아예 없던 `_workspace/`(스킬 스크래치) 항목을 추가했다.
- **서브에이전트 문단·`.claude/agents/` 항목**: 두 파일이 같은 내용을 다른 문장으로 적고 있어 `scripts/check_docs.py`가 매번 차이를 보고했다. 양쪽 모두 "위임은 Claude Code 전용, Gemini / Antigravity CLI는 같은 작업을 세션에서 직접 수행하되 정의는 규격 참고용으로 사용"이라는 동일한 문장으로 통일했다. 검사 결과 동기화 검토 대상 섹션이 2개에서 0개가 됐다.

`.claude/rules/analysis-workflow.md`와 `statsbomb-data-notes.md`는 참조하는 경로·파일이 모두 실재하고 서술도 현재 관례와 일치해 수정하지 않았다. `.claude/rules/` 내용을 본문으로 끌어올리거나 반대로 내리는 조정도 이번에는 하지 않았다.

**이유**: 마지막 검토 항목이 `2026-08-22 CLAUDE.md 검토`라 1개월 이상 지나 트리거 조건에 해당했고, 사용자가 명시적으로 검토를 요청했다. 그 사이 스페인 주제가 방법론 다섯 갈래로 확장되며 `src/` 함수 3개가 승격되고 `scripts/`에 새 파일군이 쌓였으며 `blog/`·`_workspace/`가 신설됐는데, 아키텍처 서술은 최초 셋업 시점에 머물러 있었다. 360 데이터에 대한 구체적 지식(결측·`visible_area` 커버리지 등)은 아직 검증 전이므로 `statsbomb-data-notes.md`에 넣지 않고, "침투!" 주제의 2단계 데이터 검토에서 실측한 뒤 기록하기로 했다 - 미검증 내용을 규칙 파일에 넣으면 이 검토가 막으려는 "문서가 조용히 어긋나는" 상태를 직접 만드는 셈이기 때문이다.

## 2026-09-08 침투 조작적 정의 오류 - 오프사이드를 고려하지 않아 정반대를 측정했다

**결정**: "침투!" 주제의 침투 선택지를 **"패스 시점에 상대 최종 수비 라인보다 앞에 있는 아군 수"**로 정의했던 것을 폐기하고, **"패스 시점에 온사이드(라인 이하)이면서 라인에서 5m 이내이고 공보다 앞에 있는 아군 수"**로 교체했다. 재는 시점을 "라인을 넘어간 뒤"에서 "넘어가기 직전"으로 옮긴 것이다. 이 오류를 지적한 것은 사용자였다 - "우리 팀 선수가 패스를 시작할 때 상대편 라인 뒤에 있냐 없냐는 오프사이드냐 아니냐로 결정나는 거 아닌가".

**이유**: 축구 규칙상 패스 시점에 최종 수비 라인보다 앞에 있는 선수는 침투 선택지가 아니라 **오프사이드 위치**다. 즉 구 정의는 "뛰어들 준비가 된 선수"가 아니라 "이미 걸린 선수"를 세고 있었고, 의도와 정반대에 가까운 것을 측정했다. 정의를 바꾸자 막혀 있던 것이 한꺼번에 풀렸다.

| | 구 정의 (라인 뒤) | 신 정의 (라인 앞, 온사이드) |
| :--- | ---: | ---: |
| 해당 구역 카메라 가시율 | 0.33 | 0.80 |
| 평균 / 0명 비율 | 0.36명 / 71.9% | 2.10명(10m 기준) / 8.1% |
| 경기 간 재현성 r | -0.14 | +0.33 (5m 기준, 부트스트랩 95% 구간 [+0.11, +0.54]) |
| 진출 여부 상관 | -0.32 | +0.31 (5m) / +0.33 (10m) |
| 슛 오즈비(교란 통제) | 1.018 (95% 구간이 0 포함) | 1.077 (10m) |

가시율이 함께 개선된 것은 우연이 아니다. 중계 카메라는 공과 수비 라인 사이는 반드시 담고 그 너머는 자주 놓치므로, 세려는 구역을 라인 뒤에서 라인 앞으로 옮기는 순간 관측 문제의 상당 부분이 사라졌다.

**놓친 경위와 반성**: 이 오류를 발견하기 전까지 세 갈래 방법론(원래 기획의 32팀 비교 / 이벤트 기반 대리 지표 / 상황 단위 전환)을 차례로 검증해 모두 실패로 판정했고, 주제를 보류하려던 참이었다. 그 과정에서 정의 오류를 가리키는 신호가 여러 번 나왔는데 전부 "데이터의 한계"로 해석했다:

- 평균 0.4명, 71.9%가 0명 - "침투 선택지"치고 비상식적으로 작은 값이었다
- 세트피스·백패스를 걷어내고 실제 전진 시도만 남겨도 값이 0.35에서 꿈쩍하지 않았다 - 지표가 의도한 현상을 따라가지 않는다는 직접적 신호였다
- 순위가 축구적으로 뒤집혔다(한국 1위, 스페인 최하위)
- 측정 잡음(카메라 가시율)이 축구 변수보다 결과와 더 강하게 연관됐다

즉 데이터 검토(결측률·커버리지·표본 크기·재현성)는 충실히 했지만, 그 모든 검증이 **정의가 옳다는 전제 위에서** 돌아가고 있다는 점을 보지 못했다. 검증이 정교할수록 엉뚱한 것을 정밀하게 재고 있었다.

**재발 방지**: [`.claude/rules/analysis-workflow.md`](./.claude/rules/analysis-workflow.md)의 2단계를 "조작적 정의 검토와 데이터 검토"로 확장하고 **2-1. 조작적 정의 검토** 절을 신설했다. 여섯 가지 절차(한 문장으로 적어 사용자에게 확인받기 / 경기 규칙 대조 - 특히 오프사이드 / 예상 크기 사전 기록 / 대안 정의 최소 하나 병행 계산 / 극단값 장면 눈으로 보기 / 도메인 상식과 어긋나면 데이터 탓하기 전에 정의부터 재검토)와 네 가지 위험 신호를 명시했다. `CLAUDE.md`·`GEMINI.md`의 워크플로우 요약에도 같은 취지를 반영했다. 단계 번호는 재조정하지 않았다 - `DECISIONS.md`의 과거 항목들이 "2단계 = 데이터 검토"를 전제로 쓰여 있어, 번호를 밀면 이력 기록과 어긋나기 때문이다.

`.claude/rules/statsbomb-data-notes.md`의 360 절에서 "팀 단위 집계는 3경기로 재현되지 않는다"고 단정했던 서술도 함께 바로잡았다. 그건 데이터의 성질이 아니라 잘못된 정의의 성질이었고, 그대로 두면 다음 360 주제가 같은 함정에 빠지는 대신 아예 시도조차 하지 않게 만들 수 있었다.

## 2026-09-08 "침투!" 주제 착수 - `korea_qatar2022/` 폴더 생성

**결정**: 2단계를 마치고 `korea_qatar2022/` 폴더와 [`PLAN.md`](./korea_qatar2022/PLAN.md)를 만들었다. `ideas/backlog.md`에 `진행중` 절을 신설하고 해당 항목을 옮겼다(이 저장소에서 `진행중` 상태가 실제로 쓰인 첫 사례다). 지표는 재현성이 가장 높았던 "라인 앞 5m 온사이드" 버전을 기본으로 하고, 라인 앞 10m 버전과 오프사이드 위치 선수 수(구 정의)를 보조·대조 지표로 함께 계산한다.

**이유**: 정의 교체로 세 가지 판정 기준(카메라 가시율, 경기 간 재현성, 결과 예측력)을 모두 통과했고, 순위도 축구적으로 납득 가능해졌다(상위 아르헨티나·일본·스페인·브라질, 하위에 한국). 접었던 확장 질문 3번(양 x 밀도 2차원)은 오히려 되살렸는데, 새 지표가 상대 수비 라인 깊이와 r=+0.82로 얽혀 있어 "영역 우위"와 "침투 밀도"를 두 축으로 분리해 보여주는 것이 이 교란을 감추지 않고 드러내는 방식이기 때문이다. 확장 4번은 검토 중 발견한 대조 - 한국이 라인 앞 준비 선수는 분포 최하단(10m 기준 최하위 1.60명, 5m 기준 하위 3위 0.873명)인 반면 오프사이드 위치 선수는 32팀 중 2위(0.467명, 1위는 벨기에 0.484명)라는 점 - 를 반영해 다시 썼다.

2단계 검토 산출물인 `scripts/review_penetration_360_data.py`는 최초 작성본이 48경기에 약 10분 걸려 성능을 고쳤다(583초 -> 21초, 결과 26,237행 동일 검증). 이벤트 캐시가 `to_parquet` 실패를 삼켜 매 실행마다 전 경기를 다시 받던 것, 상황마다 프레임 전체를 선형 스캔하던 것, 230만 행을 `iterrows`로 훑던 것이 원인이었다. `data/raw/`는 360 캐시(약 280 MB)가 커밋되지 않도록 `.gitignore`에 추가했다.

## 2026-09-08 관측 완전성 보정을 방법론에 필수로 추가 - 프리즈프레임 눈검증이 잡아낸 두 번째 결함

**결정**: 침투 선택지 팀 비교에 **관측 조건 보정**을 필수 절차로 넣었다. 상황 단위로 `n_launch5`를 `n_opp_visible`·`vis_launch`·`max_vis_x`·`band15`에 회귀한 잔차로 팀 평균을 낸다. 가시율 필터(`vis_launch >= 0.7`)만 쓰던 기존 계획으로는 막히지 않는 편향이라, 필터는 보조 확인용으로 강등했다.

**이유**: `plot_freeze_frame()` 프로토타입으로 극단값 장면을 눈으로 검증하다가(워크플로우 2-1의 다섯 번째 절차) 한국-우루과이전 88분 장면에서 **추정 오프사이드 라인이 x=39.8, 즉 한국 진영에 잡힌 것**을 발견했다. 우루과이 수비 라인이 화면 밖이라 "보이는 것 중 가장 깊은 상대"가 실제 최종 수비수가 아니었던 것이다. 그런데 그 장면의 `vis_launch`는 0.79로 높았다 - 이 지표는 **추정된** 라인 주변만 보므로 추정 자체가 틀린 경우를 잡지 못한다.

정량화하니 편향이 컸다. 프레임에 보이는 상대 필드플레이어가 1명일 때 침투 선택지 평균은 0.42명, 9명일 때는 1.44명이다(추정 라인 x도 50.9 -> 83.5로 이동). 그리고 **한국은 32팀 중 상대 선수가 가장 적게 관측된 팀**이었다(경기 평균 5.5명, 32팀 평균 7.7명).

보정 결과 앞서 보고했던 한국 관련 결론이 뒤집혔다.

| 지표 (낮은 쪽부터 순위) | 원시값 | `n_opp>=8` 필터 | 관측 조건 보정 |
| :--- | ---: | ---: | ---: |
| 침투 선택지 5m | 3위 | 10위 | **18위** |
| 침투 선택지 10m | 1위 | 3위 | **11위** |
| 오프사이드 위치 | 31위 | - | **8위** |

즉 "한국은 라인 앞 준비 선수가 32팀 중 가장 적고 오프사이드 위치 선수는 가장 많았다"는 대조는 **관측 조건을 보정하면 사라진다.** 보정 전후 팀 순위 상관은 0.80이고 12개 팀이 5계단 이상 이동한다. `ideas/backlog.md`의 확장 4번과 `korea_qatar2022/PLAN.md`의 해당 서술을 사실 진술에서 열린 질문으로 고쳤다.

**살아남은 것**: 지표 자체는 여전히 유효하다. 경기 간 재현성은 보정 후에도 +0.27, 진출 여부와의 상관도 +0.27로 유지되고, 보정 후 상위권(세네갈·브라질·스페인·일본·독일)과 하위권(코스타리카·이란·폴란드·모로코)도 축구적으로 납득 가능하다. 오프사이드를 반영한 정의 교체(같은 날 앞선 항목)가 옳았다는 것도 그대로다. 달라진 것은 **한국이 이 지표의 이상치가 아니라는 점**이다.

**교훈**: 이번에도 워크플로우 2-1의 "극단값 장면을 눈으로 본다"가 통계만으로는 안 보이던 결함을 잡아냈다. 두 번 연속으로 같은 절차가 작동한 셈이라, 그림으로 확인 가능한 데이터에서는 이 단계를 건너뛰지 않는다. 아울러 **관측 조건을 재는 지표(여기서는 `vis_launch`)가 통과했다고 해서 관측이 충분하다는 뜻은 아니라는 것** - 그 지표가 추정값에 의존한다면 추정이 틀린 경우를 통과시킨다 - 도 함께 기록해둔다.


## 2026-09-10 프리즈프레임 대비 스냅샷 - "전형" 선정 기준을 중앙값에서 평균으로 변경

**결정**: `korea_qatar2022/PLAN.md`의 후속 선택 작업 중 "프리즈프레임 대비 스냅샷"을 착수했다(`korea_qatar2022/05_snapshot_contrast.py`). PLAN 원안은 "각 팀 침투 선택지 개수의 **중앙값**에 가장 가까운 상황"을 전형 장면으로 뽑으려 했으나, 이 기준을 **평균**으로 바꿨다. 산출물은 `processed/fig_snapshot_argentina.png`, `fig_snapshot_korea_group.png`, `fig_snapshot_korea_brazil.png`, `snapshot_notes.md`.

**이유**: 데이터를 확인하니 한국·아르헨티나·브라질·일본·스페인 등 주요 여섯 팀의 `n_launch5` 중앙값이 모두 1로 겹쳤다(분포가 0/1/2에 쏠려 있어). 중앙값 기준으로는 어느 팀을 뽑아도 청록 링 1개짜리 장면이 나와 팀 차이를 담지 못한다. 팀 차이는 평균에 있다(한국 0.87 vs 아르헨티나 1.46 - 0명 상황의 빈도 차이). 사용자와 상의해 "PLAN 원안 유지, 선정 기준만 평균으로"를 택했다. 대안이었던 "관측 조건 대비 중심 재설계"와 "팀당 여러 장면 그리드"는 각각 PLAN 산출물 문구를 크게 바꾸거나 그림이 복잡해져 보류했다.

**한계(합의됨)**: 평균 기준이어도 세 장면 모두 `n_launch5=1`이라 강조 개수 자체는 비슷하다. 그래서 각 장면 캡션·`snapshot_notes.md`에 표본의 `n_launch5` 분포를 함께 적어 "그런 장면이 얼마나 자주 나오는가"의 차이를 보완한다. 대신 세 장면의 맥락(화면에 잡힌 상대 수 5명 vs 9-10명, 추정 라인 깊이 x=67.8 vs 77-80)은 크게 달라, 이 대비가 관측 조건 편향을 눈으로 보여주는 스냅샷의 핵심 역할을 한다. 동점 처리는 `n_opp_visible`을 가장 먼저 맞춰(이 주제의 핵심 변수) 표본 중앙값에 가까운 상황을 고르며, 전형 장면이 90분+ 늦은 시각에 걸릴 수 있다(점유 기반 지표라 후반이 특별히 비대표적이지는 않음, 부제에 정확한 시각 표기).

`situations.pkl`에 event_id가 없어, `match_id`+`team`+`minute`+`second`+`type=='Pass'`로 후보를 좁힌 뒤 `situations.ball_x`(= `round(event.location[0], 2)`)로 원본 이벤트를 유일하게 특정하는 방식으로 frames를 재링크했다. 재계산한 `def_line`이 저장값과 0.05 이내인지 assert로 검산한다.

## 2026-09-10 블로그 원고: "비전문 일반인이 쉽게 읽을 수 있는 수준"을 명시 규칙으로

**결정**: `blog-draft`가 쓰는 `BLOG_POST.md` 원고는 **통계·데이터 분석·축구 데이터 전문 용어를 모르는 일반 독자가 한 번에 읽고 이해할 수 있는 수준**으로 쓴다. `.claude/agents/blog-draft.md`의 "작성 규칙"에 전용 항목을 추가하고(용어 치환 예시 포함: "원시값" -> "보정 전 수치", "상관" -> "함께 움직이는 정도"+한 줄 설명, "잔차/회귀/분산/선형 외삽/통제" 등도 일상어로, "가시율" -> "화면에 잡히는 비율", 좌표값은 눈금 설명 후 "위치값 XX"), `CLAUDE.md`·`GEMINI.md`·`.claude/rules/analysis-workflow.md`의 blog-draft 서술에도 같은 취지를 반영했다.

**이유**: korea_qatar2022 블로그 원고 1차본이 "데이터 저널리즘 톤"·"일반 독자용"이라는 기존 규칙을 지켰는데도 "원시값", "회귀한 잔차를 팀 평균으로", "지표 분산의 13%를 설명", "선형 외삽에 기댑니다" 같은 표현이 남아 사용자가 두 차례("원시값 대신 쉬운 표현", "비전문 일반인 수준으로 조건 명시") 수정을 요청했다. 기존 규칙이 톤(질문 던지기, 방법론 따라가기)은 규정하지만 어휘 난이도는 규정하지 않아, 원고가 형식상 규칙을 지키면서도 일반 독자에게 어려울 수 있었다. 판단 기준도 함께 박았다: "축구는 즐겨 보지만 데이터 분석은 해본 적 없는 지인이 읽고 '무슨 말인지 알겠다'고 할 수준".

## 2026-09-14 "침투" 정의 2차 확장: 레인 제한 + 파이널 서드 포함, 기존 결과는 덮어쓰기

**결정**: `korea_qatar2022`의 침투 지표를 **채널 침투 선택지**로 확장한다 - 기존 조건(온사이드, 라인 앞 5m, 공보다 앞)에 **레인 제한(사이드 y<18 또는 y>62, 하프스페이스 18~30·50~62)**을 추가하고, 대상 구간을 **x 30~80에서 30~110으로** 넓힌다(x 110 이상 바이라인은 제외). 중앙(y 30~50) 카운트는 버리지 않고 **대조군으로 유지**한다. 그리고 **기존 1차 정의 결과를 병행 보존하지 않고 덮어쓴다** - 방법론별 하위 폴더로 나누지 않는다.

**이유**: 사용자가 축구적 근거로 지적했다 - "컷백을 받으러 들어가는 움직임은 침투가 아니다. 컷백을 올리기 위해 풀백/윙어가 사이드나 하프스페이스로 들어가는 움직임이 침투다." 1차 정의는 x축만 봐서 이 둘을 구분하지 못했고, 실제로 레인별로 분해하니 파이널 서드로 갈수록 **중앙 인원이 0.31 -> 1.08로 세 배 늘어 지표를 밀어 올리고 있었다**(사이드는 0.36 -> 0.44로 평평). 또 대상 구간 상한 80이 정확히 파이널 서드 경계라, 침투가 가장 많이 나와야 할 구간이 통째로 빠져 있었다. 로우 블록 상황(`def_line >= 100`)은 기존 범위에 545건뿐이지만 x 80~110을 열면 5,253건으로 10배가 된다.

수치로도 뒷받침됐다. 같은 x 범위(80-110)에서 레인만 제한하면 표본이 절반이 되는데도 경기 간 재현성이 **0.188 -> 0.258**로 오르고(같은 표본 크기의 중앙 지표는 0.193), 최종 후보인 x 30-110 + 채널 제한은 1차 정의 대비 재현성 **0.319 -> 0.447**, 16강 진출 상관 **0.316 -> 0.363**으로 둘 다 개선됐다. 밴드 폭은 넓힐수록 재현성이 단조 감소해(5m 0.258 / 10m 0.201 / 15m 0.132 / 무제한 0.104) 5m를 유지했다.

**대안 검토 - 상대 수비 배치 기준 "동적 경계"는 폐기**: 축구적으로 가장 정확하지만 **360으로는 백4와 백5를 구분할 수 없다.** 선발 백라인 인원(3 vs 4)과 프레임에 보이는 수비 수의 상관이 **-0.065**, 수비 간 최대 간격과의 상관이 **0.005**인 반면, **보이는 수비 수와 최대 간격의 상관은 -0.346**이었다. 즉 동적 경계가 노리는 "수비 사이 틈"은 상대 배치가 아니라 화면에 몇 명이 잡혔는지로 결정된다(안 보이는 수비수 자리에 가짜 틈이 생긴다). 이를 채택하면 이 프로젝트가 이미 겪은 관측 편향을 지표 정의 한복판에 끌어들이게 되므로, 피치 라인 기반 고정 경계를 쓴다(y=18/62는 페널티박스 폭, y=30/50은 골에어리어 폭).

**덮어쓰기를 택한 이유**: `analysis-workflow.md` 3단계는 한 주제에 방법론이 여러 갈래면 하위 폴더로 나누도록 허용하지만(`spain_euro2024`가 그 예), 이번 건은 갈래가 아니라 **교체**다. 사용자 판단은 "1차 정의는 침투를 잘못 조작화했으므로, 리소스가 더 들더라도 덮어쓰는 편이 낫다"였다. 두 결과를 나란히 두면 어느 쪽이 맞는 값인지 혼란만 남는다. 기존 분석은 커밋 `40f7031`~`84b5bd3`에 남아 언제든 꺼낼 수 있으므로 정보 손실은 없다.

**함께 정리한 것**:
- **분석 질문의 프레이밍 교정**: 기존 질문 4가 "한국의 원시값 극단은 실제 경기 내용인가 중계 화면 구성의 산물인가"로 결론을 일부 전제하고 있었다. 사용자 지적대로 이 주제의 목적은 "2022 한국의 침투가 어땠는가"를 재는 것이지 "한국은 침투가 적은 팀"을 확인하는 것이 아니므로, 질문을 중립적으로 고치고 PLAN에 명시 문구를 넣었다.
- **관측 조건 보정 재적합이 필수**: 현재 `vis_launch`는 라인 앞 10m를 y 전 폭으로 재서 레인을 구분하지 못한다. 카메라가 공을 따라가므로 터치라인 쪽이 더 자주 화면 밖으로 나가고 그 정도가 팀마다 다를 수 있어, `vis_launch_wide`/`vis_launch_half`를 따로 계산해 공변량에 넣어야 한다.
- **블로그 보류**: `blog/korea_qatar2022/BLOG_POST.md`는 1차 정의의 "보정 전 3위 -> 보정 후 18위" 서사 위에 서 있어 재실행 후 무효가 된다. 파일은 문체·구성 참고용으로 남기고 재실행 완료 후 `blog-draft`를 다시 돌린다.
- **`ideas/backlog.md` 상태를 `완료` -> `진행중`으로 되돌렸다.**

**한계로 남긴 것**: 지표가 낮은 팀에 성격이 반대인 두 유형이 섞인다 - 모로코(4강)는 밀어붙일 일이 없어서 낮고, 포르투갈(8강)은 밀어붙이는데 못 뚫어서 낮다. 포르투갈은 5경기 12골 중 필드골이 4골(33.3%, 대회 평균 38.2%)이고 PK가 2골(16.7%, 대회 평균 7.6%)이며, **오픈 플레이 흐름에서 채널을 파고들어 컷백으로 만든 골은 0골**이었다. PK 제외 xG 5.75에 10골로 +4.25 초과 달성했고 12골 중 6골이 스위스전 한 경기에 몰려 있다. 이 구분은 질문 3의 "양 x 밀도" 2차원 평면이 맡는다.

## 2026-09-15 "침투" 2차 확장 재실행 완료 - 중앙 대조군이 반대 방향으로 갈렸다

**결정**: 2026-09-14에 확정한 채널 침투 선택지 정의로 `korea_qatar2022`를 전면 재실행하고, `RESULTS.md`를 새로 썼다. `ideas/backlog.md`는 다시 `완료`로 옮겼다. 블로그만 여전히 1차 정의 서사라 보류 상태다.

**과정에서 정한 것 세 가지**:

1. **공변량에서 전 폭 가시율을 레인별 가시율 3개로 교체했다.** 네 세트(기존 4개 / 기존+사이드·하프 / 전 폭 빼고 레인 3개 / 전부)를 비교했더니 레인을 넣은 세 세트는 팀 순위 상관이 1.0000으로 사실상 같은 결과를 냈다. 그렇다면 `vis_launch`와 `vis_launch_wide`·`vis_launch_half`의 상관이 0.898·0.934인 것을 두 번 넣을 이유가 없어, 레인만 명시적으로 넣은 세트를 골랐다. 가시율끼리 상관이 높아 개별 회귀계수는 해석하지 않는다 - 쓰는 것은 잔차뿐이다.

2. **레인별 가시율 차이가 실측으로 확인됐다**: 사이드 0.690 / 하프스페이스 0.865 / 중앙 0.931. 채널 지표가 세는 두 레인이 정확히 가장 안 보이는 구역이고, 전 폭 0.800은 이 차이를 중앙으로 희석해 가린다. `statsbomb-data-notes.md`의 해당 항목을 "다를 수 있다"에서 실측값으로 고쳤다.

3. **`plot_freeze_frame()`에 `lane_split` 인자를 추가**했다(기본 False라 기존 산출물은 불변). 레인 경계와 채널(실선)·중앙(점선) 구분을 그린다. 05번 스냅샷의 전형 장면 선정에서 **킥오프 직후(전·후반 시작 3분)를 후보에서 제외**하는 규칙도 넣었다 - 검증 중 포르투갈 전형이 0:11로 뽑혔는데 킥오프 대형은 그 팀이 경기를 어떻게 풀었는지를 대표하지 않는다. 평균 계산에서까지 빼면 지표 정의와 어긋나므로 후보에서만 뺀다.

**가장 중요한 결과 - 중앙 대조군의 부호가 뒤집힌다.** 채널 침투 선택지는 16강 진출과 보정 후 +0.34로 유지되는데, 같은 조건으로 중앙(골에어리어 폭)에 선 인원은 원시 +0.12에서 보정 후 **-0.13**이 된다. 보정 전후 순위 상관도 0.62로 세 지표 중 가장 낮아 중앙 카운트가 관측 조건에 가장 휘둘린다. 즉 사용자가 지적한 "컷백을 받으러 들어가는 움직임은 침투가 아니다"가 데이터에서도 **방향이 반대인 두 지표**로 갈렸다. 1차 정의는 이 둘을 합쳐 세고 있었다.

**한국에 대한 답이 달라진 부분**: 원시 순위가 1차 정의만큼 극단적이지 않다(채널 8위, 1차에서는 3위). 보정 후 15위로 중위권인 것은 같고, 중앙은 보정 후 25위로 오히려 평균 위쪽이다. 질문 3의 2차원 평면에서는 포르투갈·잉글랜드와 같은 사분면(양 많음 + 밀도 낮음)에 들어가지만 양·밀도 모두 중앙값 바로 옆이라 경계선에 가깝다.

**`processed/*_notes.md`를 스크립트 생성물로 바꿨다.** 질문 1~5의 관찰 메모 네 개는 손으로 쓴 문서라 재실행 뒤에도 2026-09-09자 1차 정의 수치가 그대로 남아 있었다(`RESULTS.md`가 상세 수치 출처로 참조하는 파일들이다). 05번 스냅샷 메모처럼 각 스크립트가 계산값으로 매번 다시 쓰게 하고, 파일 상단에 "손으로 고치지 말 것"을 박았다. 해석 문장은 남기되 수치는 전부 변수로 뺐다.

**재현 가능성을 코드에 박았다**: 스크래치패드에서 일회성으로 돌렸던 검토 (1)~(6)을 `scripts/review_penetration_lanes_data.py`로 옮기고, 스크립트 끝 `[J]` 절이 2026-09-14 값을 상수로 대조하게 했다(34개 항목, 건수 5% / 평균·상관 0.03 기준). 재현 과정에서 (5)(6)의 대상 범위가 x 30-80이 아니라 x 30-110이었음이 드러나 PLAN의 해당 표를 고쳤다. 아울러 그림·콘솔 출력에 손으로 박혀 있던 1차 정의 수치(한국 "3위 -> 18위", "12개 팀 이동", 진출 상관 "+0.27", 브라질전 "상승/하락" 라벨)를 전부 데이터에서 계산하도록 바꿨다 - 실제로 재실행 후 02번 그림 제목이 "+0.27에서"로 남아 조용히 틀린 값을 표시하고 있었다.

## 2026-09-16 블로그 글쓰기 스킬 `blog-essay-architect` 도입 - 초안 구성 기준으로 명시

**결정**: 사용자가 정리한 블로그 글쓰기 프레임워크를 프로젝트 스킬 `.claude/skills/blog-essay-architect/SKILL.md`로 두고, `blog-draft` 에이전트가 초안을 쓸 때 이 스킬의 6단계 서사(Hook -> 핵심 발견 -> 반전 -> 2차원 분류 -> 방법론 비하인드 -> 한계)를 따르도록 `blog-draft.md`·`analysis-workflow.md`·`CLAUDE.md`·`GEMINI.md`에 명시했다.

**위치를 `.claude/skills/`로 정한 이유**: 루트에 두면 Claude Code가 스킬로 인식하지 못하고, `.claude/rules/`는 세션 규칙용이라 성격이 다르다. `.claude/skills/<이름>/SKILL.md`는 Claude Code가 프로젝트 스킬로 자동 인식하는 표준 위치이고, `blog-draft`·Gemini / Antigravity CLI는 경로로 직접 읽으면 된다. 옮기면서 스킬 인식에 필요한 frontmatter(`name`, `description`)를 붙이고, 파일 전체를 감싸던 ```` ```markdown ```` 코드 펜스와 다른 도구에서 붙어 온 `[cite: 1]` 인용 표식을 지웠다. 본문은 바꾸지 않았다.

**기존 규칙과의 조정**: 기존 `blog-draft` 문체 규칙은 "방법론을 하나씩 따라가며"였는데, 스킬은 논문식 시계열 구성을 지양 패턴으로 꼽는다 - 구성은 스킬을 따르도록 교체했다. 반면 수치 원천(`RESULTS.md`만), 일반 독자용 용어 풀이(스킬 예시의 "원천 데이터" 같은 표현 포함), 하이픈, `blog-html` 마커 규칙은 에이전트 문서가 우선한다고 적었다. 스킬 예시 문장은 `korea_qatar2022` 수치로 쓰였지만 톤·구조 예시일 뿐 수치 출처가 아니므로 그대로 옮기지 않게 했다. `spain_euro2024` 원고는 이제 형식(마커·경로·주석) 템플릿으로만 참고한다.

**스킬 본문 보강 - 수식어 규칙**: 예시가 수치 출처가 아니어도, 예시가 가르치는 말투는 그대로 옮겨진다. 실제로 예시에 상관 결과를 인과로 읽히게 하는 동사("견인했지만")와 데이터보다 센 수식어("압도적으로", "단숨에", "정확히")가 있었다. 수식어를 빼면 흥미가 떨어진다는 사용자 의견에 따라, 약화하는 대신 **비교 수치로 강조를 유지**하는 방향으로 예시 5곳을 고치고 4절에 수식어 규칙 3줄(강도는 수치만큼, 상관에 인과 동사 금지, 흥미는 대비·반전으로)을 넣었다. 예시가 `korea_qatar2022` 기반이라 같은 주제로 원고를 쓸 때(다음 블로그 재작성이 바로 그 경우) 세부 설명이 따라 들어갈 수 있어, 3절 머리에 "예시를 옮기지 말고 `RESULTS.md`와 대조" 안내를 넣었다. 예시의 남은 세부 오류("채널(하프스페이스)", "가장 적게 잡힌 경기")는 이 안내와 발행 전 사실 대조에 맡기고 고치지 않았다.

**이어서 - 예시 사실 교정 + 용어 풀이 기준 변경**: 같은 날 남겨 뒀던 세부 오류도 고쳤다. Step 1에 2026 해설과 2022 데이터의 시점 차이를 넣고 "승리의 조건"을 16강 진출 기준으로, Step 2의 채널을 "사이드와 하프스페이스"로, Step 3의 "가장 적게 잡힌 경기"를 "팀"으로·"채널 구역"을 "사이드"로, Step 4의 RESULTS.md에 없는 해석("내려앉아 역습하느라")을 전진 횟수 수치로, Step 6의 "승리"를 "16강 진출"로 바꿨다. 이때 **용어 풀이 기준을 바꿨다**: 사용자 판단으로 "블로그 독자는 축구를 즐겨 보는 사람이므로 하프스페이스·컷백 같은 축구 용어는 풀 필요가 없고, 통계 지식을 요구하는 용어만 푼다." 기존 규칙은 "통계·데이터·축구 전문 용어를 풀어 씀"이고 오픈플레이에도 괄호 설명을 달게 했으므로, 스킬·`blog-draft.md`·`analysis-workflow.md`·`CLAUDE.md`·`GEMINI.md`를 같은 기준으로 맞췄다. 다만 "가시율", "필드플레이어", 좌표값처럼 데이터 수집 과정에서 생긴 용어와 분석에서 만든 지표 이름은 축구 팬 상식이 아니므로 계속 풀어 쓴다.

## 2026-09-16 블로그에서 분석 과정 서술을 뺀다 - 글쓰기 스킬 6단계 -> 5단계

**결정**: 블로그 원고에는 분석 과정에서 겪은 어려움과 그 극복 과정(원천 데이터의 문제와 보정 방법, 조작적 정의를 바꾼 이력, 처음 분석 대비 수치·순위 변화)을 쓰지 않는다. 이런 내용은 `DECISIONS.md`·`PLAN.md`·`RESULTS.md`에만 남기고, 블로그는 최종 결과만 제시한다. 이에 따라 `blog-essay-architect` 스킬에서 Step 5 "Behind the Scenes(방법론과 실패 극복기)"를 없애 5단계(Hook -> 핵심 발견 -> 반전 -> 2차원 분류 -> 한계)로 줄이고, `blog-draft.md`·`analysis-workflow.md`·`CLAUDE.md`·`GEMINI.md`를 같이 고쳤다.

**이유**: 사용자 판단 - "독자들은 분석 과정에서 어떤 어려움이 있어서 어떻게 극복했는지까지 궁금하지 않다." 기존 스킬은 Step 5에서 시행착오 고백을 권하면서 지양 패턴 3에서는 고생 과시를 금지해 경계가 모호했는데, 이번 결정으로 그 충돌도 없어졌다.

**함께 바뀐 것**:
- **Step 3(반전)의 성격**: 기존 예시는 "보정 전 8위 -> 보정 후 15위"라는 분석 전후 변화로 반전을 만들었다. 이제 반전은 **통념·해설의 인상 vs 최종 결과** 사이에서만 만들고, 최종값만으로 예시를 다시 썼다(한국 채널 15위, 중앙 25위). 통념과 어긋나는 결과가 없으면 반전을 억지로 만들지 않는다는 조건도 넣었다.
- Step 2 예시에서 "중계 화면 조건을 감안했을 때"를 뺐다. 최종값만 쓰므로 보정을 언급할 이유가 없다.
- 핵심 원칙에 "Results, Not Process"를 추가하고, "Domain Intuition" 원칙은 "숫자의 함정을 바로잡은 과정"이 아니라 "숫자가 경기에서 무엇을 뜻하는지 해석"으로 바꿨다.
- 용어 풀이 목록에서 "원시값 -> 보정 전 수치"를 뺐다. 보정 전 수치 자체를 쓰지 않으므로 필요 없다.
- 결과를 읽는 데 필요한 방법론("무엇을 셌는지" 한두 문장)과, 최종 결과에 남는 한계(예: 중계 화면에 잡힌 선수만 기록된다)는 계속 쓴다. 한계는 그 내용만 밝히고, 분석 중에 어떻게 발견·처리했는지는 쓰지 않는다.
- **한계의 위치**: 사용자 판단으로 한계는 유지하되 **본문 중간에 단서로 끼워 넣지 않고 글 마지막 한계 섹션에만 짧게**(핵심 2-3개) 쓴다. 본문 곳곳의 단서가 흐름을 끊기 때문이다.

## 2026-09-16 글쓰기 스킬 마무리 정리 - 프로젝트 전용 절, 요약 섹션 폐지, 볼드 상한

**결정**: `blog-essay-architect` 스킬 리뷰에서 남은 항목을 반영했다.

- **Step 2 대비 요소는 근거가 있을 때만**: Step 3과 같은 이유로, "대비 요소를 발굴해 제시"를 "결과 안에 대비가 있으면 부각, `RESULTS.md`에 근거가 없으면 결론만 담백하게"로 바꿨다. 대비가 없는 주제에서 대비를 만들어내다 과장하는 것을 막기 위함이다.
- **스킬에 6절 "이 프로젝트에서 쓸 때" 추가**: 수치 출처, 하이픈, 그림 경로·캡션, HTML 마커, 제목 후보, 원고 이후 흐름처럼 `blog-draft.md`에만 있던 규칙의 요점을 넣었다. 메인 세션이나 Gemini / Antigravity CLI가 `blog-draft` 없이 스킬을 직접 써도 같은 규칙이 적용되게 하려는 것이다. 규칙 전문은 복사하지 않고 `blog-draft.md`를 원본으로 가리켜, 두 곳을 따로 관리하지 않게 했다.
- **`결론부터`·`종합하면` 요약 섹션 폐지**: `blog-draft.md` 섹션 번호 규칙이 `## 종합하면`을 두게 했고 형식 템플릿 `blog/spain_euro2024/BLOG_POST.md`도 서두·말미 요약 구조였는데, 이는 스킬의 No Redundant Summaries(서두 요약 - 본문 - 말미 요약의 3단 반복 금지)와 정면으로 부딪힌다. 번호 없는 섹션은 마지막 `## 한계` 하나로 정하고, 템플릿은 형식만 참고하며 요약 섹션 구성은 따르지 않는다고 명시했다. `blog-html.md`에는 "종합하면"에 의존하는 매핑이 없어 변환에는 영향이 없다. 이미 발행한 `spain_euro2024` 글은 고치지 않는다.
- **볼드는 한 문단에 1개 이내**: 볼드가 많으면 핵심이 흐려지고, `humanize-korean` 윤문에서 AI 티 패턴으로 잡혀 깎이기 때문이다.

## 2026-09-16 블로그 글쓰기 스킬 적합성 검토 단계 도입 - 일반화는 보류

**결정**: `blog-essay-architect` 스킬을 다른 주제에도 쓸 수 있게 일반화하는 작업은 **보류**하고, 대신 원고 작성 전에 메인 세션이 **스킬 적합성 검토**를 하도록 했다. 주제의 `RESULTS.md` 핵심 발견을 5단계에 배치한 표(`blog/<주제>/ARC_MAP.md`)를 만들고, 판정 기준(비는 단계 2개 이상 / 자리 없는 핵심 발견 / 없는 통념·대비를 만들어야 함)에 걸리면 원고 전에 스킬을 고친다. 적합하면 배치표를 `blog-draft`에 넘겨 설계도로 쓴다. 스킬 2.5절, `analysis-workflow.md` "블로그 발행 전 검토" 0단계, `blog-draft.md` 착수 전 읽기 6번, `CLAUDE.md`·`GEMINI.md`에 반영했다.

**이유**: 스킬을 완료된 두 주제에 대 보니 문장 규칙·원칙은 주제와 무관하지만 5단계 구조는 `korea_qatar2022` 형태에 맞춰져 있었다. 사용자 판단은 "아직 해 보지 않은 주제를 상상해서 고치기보다, `korea_qatar2022` 원고를 먼저 쓰고 실제 새 주제가 생겼을 때 그 주제에 대 보고 고치는 편이 낫다"였다. 적합성 검토는 그 시점을 놓치지 않게 하는 장치다.

**보류한 일반화 문제 (다음 주제의 적합성 검토에서 여기서 출발할 것)**:
1. **Step 3·4가 이 주제 전용 장치다.** Step 3은 "여러 대상 중 핵심 주체 조명"이라 단일 대상 주제(`spain_euro2024`)에 맞지 않고, 스페인의 진짜 반전인 "그럴듯한 설명(득점 효율)의 기각"을 넣을 자리가 없다. Step 4의 2x2 분류도 고유 장치라, 스페인의 예외 사례(프랑스전)·일관성(7경기 유지)이 들어갈 칸이 없다. 안: Step 3을 "통념 뒤집기(핵심 대상의 위치 / 설명의 기각 / 겉보기와 실제)", Step 4를 "단편적 해석 막기(2차원 분류 / 예외 사례 / 일관성)"로 역할 중심 재정의.
2. **Step 1 Hook이 통념 검증형만 가정한다.** 검증할 통념이 없는 탐색형 주제에서는 가짜 통념을 만들게 된다. 안: 장면형·질문형 Hook 추가.
3. **예시가 한 주제뿐이다.** 모델이 "팀 순위 비교" 형태를 정답으로 배운다. 안: Step 1·3·4에 스페인 예시 한 문장씩 추가.
4. **방법론이 여러 개인 주제의 구성 안내가 없다.** 실제 스페인 원고가 방법론 순서로 섹션을 나눴다. 안: 방법론이 아니라 발견 기준으로 묶기. 또 패스 네트워크처럼 읽는 법이 필요한 그림은 "분석 과정 서술"이 아니므로 그림 앞이나 캡션에 읽는 법을 써도 된다고 구분.
5. **"차이 없음" 결과와 통계 검정 용어.** 대비·반전을 기대하는 구조라 "차이 없음"이 결론인 주제가 약해 보이거나 과장될 위험이 있다. 안: 기각된 가설도 발견이라는 안내. 용어 목록에 "통계적으로 유의" -> "우연으로 보기 어려운", p값 풀이 추가.

## 2026-09-21 `korea_qatar2022` 블로그 스냅샷은 새로 뽑는다 - 전형 장면 대신 설명용 예시

**결정**: 블로그 Step 2("무엇을 셌는지")에 쓸 프리즈프레임 스냅샷은 기존 `fig_snapshot_argentina.png`·`fig_snapshot_korea_group.png`를 쓰지 않고 새로 한 장 만든다. 한국 조별리그 세 경기 안에서 채널·중앙·오프사이드 위치 선수가 한 장면에 모두 있고 화면에 잡힌 상대가 9명 이상인 장면(후보 8건)을 고르며, 캡션은 "전형"이 아니라 "예시 장면"으로 쓴다. 그림 안 문구는 분석용 그대로 두기로 해, 09-16에 올려 둔 세 선택지(블로그 모드 인자 / 문구 덮어쓰기 / 스냅샷 제외)는 모두 폐기했다. 장면 선택과 범례 보완 여부는 사용자 확인 대기(`korea_qatar2022/PLAN.md` "블로그용 스냅샷 재생성").

**이유**: 기존 스냅샷은 분석용으로 팀별 평균에 가장 가까운 장면을 뽑아서, 아르헨티나와 한국이 둘 다 채널 1 · 중앙 0으로 같게 나왔다. 두 장면이 실제로 다른 점은 관측 조건(보이는 상대 10명 vs 6명)인데, 이는 새 블로그 규칙상 쓰지 않는 보정 이야기다. 블로그에서 스냅샷이 할 일은 팀 비교가 아니라 "세는 선수 / 비교용 선수 / 세지 않는 선수"를 한 장에 보여 주는 것이라 선정 기준 자체가 달라야 한다. 한국으로 한정한 것은 글의 주인공이라 몰입이 쉽기 때문이고, 브라질전을 뺀 것은 글의 중심이 조별리그 비교이기 때문이다. 기존 `fig_snapshot_*.png`와 `05` 스크립트의 전형 선정 로직은 분석 기록으로 그대로 둔다.

## 2026-09-22 `korea_qatar2022` 블로그 스냅샷 확정 - 가나전 75:22, 범례 보완, 보로노이 제외

**결정**: 09-21 후보 8장 중 가나전 75:22(황인범 -> 정우영, 채널 2 / 중앙 1 / 오프사이드 위치 2)를 `processed/blog_snapshot.png`로 확정했다. 선정 로직과 선택값은 `05_snapshot_contrast.py`에 정식 반영했다. `plot_freeze_frame()` 범례에 주황 링 "오프사이드 위치" 항목을 추가했다. 2026-09-11부터 미결이던 보로노이 오버레이는 넣지 않는다.

**이유**: 가나전 75:22는 세 종류 선수가 서로 떨어져 있고 링 겹침이 적어 "세는 선수 / 비교용 선수 / 세지 않는 선수"를 한 장에서 가장 잘 구분해 보여 준다. 범례에 주황 링이 없으면 독자가 오프사이드 위치 표시의 뜻을 알 수 없다. 보로노이는 레인 경계 위에 얹으면 그림이 복잡해지고, 360은 화면에 잡힌 선수만 기록하므로 안 보이는 선수 자리가 가짜 빈 공간으로 그려진다 - 수비 배치 기준 경계를 쓰지 않기로 한 것과 같은 이유다(`statsbomb-data-notes.md`).
