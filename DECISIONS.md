# DECISIONS.md

이 파일은 이 프로젝트에서 내린 주요 의사결정을 시간순으로 기록합니다. 새로운 결정을 내리면 아래 형식으로 항목을 추가하세요.

```
## YYYY-MM-DD 결정 제목

**결정**: 무엇을 하기로 했는지
**이유**: 왜 그렇게 결정했는지 (대안이 있었다면 함께 기록)
```

---

## 2026-08-21 CLAUDE.md 최초 작성

**결정**: `CLAUDE.md`를 신규 작성하여 빌드/실행 명령어와 아키텍처 개요(`data_loader.py` → `visualizer.py` → 스크립트/노트북 파이프라인)를 문서화했다.
**이유**: 기존에 Claude Code용 가이드 문서, Cursor/Copilot 규칙, Codex/Gemini 설정이 전혀 없어 매번 코드베이스를 처음부터 파악해야 했다.

## 2026-08-21 CLAUDE.md를 한글로 작성

**결정**: `CLAUDE.md`의 설명 문구를 전부 한글로 번역했다 (명령어, 경로, 함수명 등 코드 관련 요소는 원문 유지).
**이유**: 소스 코드 주석, 독스트링, README가 모두 한글로 작성되어 있어 문서 스타일을 프로젝트 전체와 일관되게 맞추기 위함.

## 2026-08-21 기존 GitHub 저장소(taijong915/football_analysis)에 연결

**결정**: 로컬 프로젝트를 `git init`으로 초기화한 뒤, 이미 존재하던 원격 저장소 `https://github.com/taijong915/football_analysis`를 `origin`으로 추가하고 연결했다. 새 저장소를 만들지 않았다.
**이유**: 사용자가 해당 URL의 기존 저장소를 지정했다. 기존 저장소에는 placeholder README 한 줄(`# football_analysis`)만 있었고 실질적인 코드가 없어 충돌 없이 로컬 내용을 이식할 수 있었다.

## 2026-08-21 원격/로컬 히스토리 병합 시 로컬 README를 우선

**결정**: `git merge origin/main --allow-unrelated-histories -X ours`로 병합하여, 원격의 placeholder README 대신 로컬의 상세 README(`README.md`)를 유지했다.
**이유**: 두 히스토리가 공통 조상이 없는 별개 저장소였고(unrelated histories), 유일한 충돌 파일인 README.md는 로컬 버전이 압도적으로 더 상세했다. 강제 push(`--force`) 대신 정상적인 병합 커밋으로 이력을 보존하는 방식을 택했다.

## 2026-08-21 분석 주제 아이디어를 위한 `ideas/` 폴더 신설

**결정**: 분석 주제를 브레인스토밍하고 백로그로 관리하는 `ideas/` 폴더를 만들었다. 상태(대기/구체화/진행중/완료/보류)를 가진 단일 백로그 파일(`ideas/backlog.md`)과 사용법을 설명하는 `ideas/README.md`로 구성했다. 주제별로 파일을 쪼개지 않았다.
**이유**: 아직 아이디어가 소수일 것으로 예상되어 파일을 여러 개로 나누는 것은 과도한 구조화였다. 노트북/스크립트로 옮겨가기 전 단계의 논의를 코드/문서와 분리해 `notebooks/`, `scripts/`가 실제 완료된 분석으로만 채워지도록 유지하려 했다.

## 2026-08-21 아이디어 도출 시 질문증강 방식으로 소통하도록 명시

**결정**: `CLAUDE.md`에 "아이디어 도출 시 역할" 섹션을 추가하여, `ideas/` 관련 대화에서는 아이디어를 바로 나열하지 않고 질문증강(question augmentation) 방식 — 되묻는 질문으로 사용자의 생각을 구체화시키는 방식 — 으로 소통하도록 명시했다. 단, 사용자가 명시적으로 브레인스토밍 목록을 요청하면 예외로 뒀다.
**이유**: 사용자가 아이디어 도출 과정 자체를 대화형으로 진행하길 원했다. Claude가 일방적으로 아이디어를 나열하는 대신, 질문을 통해 사용자가 스스로 관심사와 제약을 명확히 하도록 돕는 것이 목표다.

## 2026-08-21 .gitignore는 기존 파일을 그대로 사용

**결정**: 새로 작성하지 않고 프로젝트에 이미 있던 `.gitignore`(`.venv/`, `__pycache__/`, `.ipynb_checkpoints` 등 제외 규칙 포함)를 그대로 사용해 첫 커밋을 진행했다.
**이유**: 기존 `.gitignore`가 이미 이 프로젝트에 필요한 항목(가상환경, 파이썬 캐시, 노트북 체크포인트, IDE 설정)을 적절히 커버하고 있어 중복 작업이 불필요했다. `data/raw|processed`는 주석 처리된 상태로 두어 현재는 결과 이미지가 저장소에 커밋되도록 유지했다.
